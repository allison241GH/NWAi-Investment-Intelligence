# NWAi TechGroup Deal Pipeline — Architecture Overview
*v0.45.0 | August 26, 2026 | New World Angels Investment Intelligence*

---

## How It All Fits Together

Think of the pipeline like a **factory floor with six stations**. Raw material (a pitch) enters at Inbox and exits as a finished product (an Investment Memo). Each station has its own tools, workers (agents), and quality gates. Claude is the foreman — orchestrating the work, applying the frameworks, and making sure nothing advances without passing inspection.

```
┌─────────────────────────────────────────────────────────────────────┐
│                        JAMIE / DEAL LEAD                            │
│                  (commands via Claude Code)                         │
└───────────────────────────┬─────────────────────────────────────────┘
                            │  slash commands / natural language
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     CLAUDE (Orchestrator)                           │
│         Reads CLAUDE.md context + nwai-investment-framework         │
│         skill at session start. Applies NWAi investing voice.       │
└────┬──────────────────┬─────────────────┬───────────────────────────┘
     │                  │                 │
     ▼                  ▼                 ▼
  Commands           Agents            Skill
  (8 commands)    (7 subagents)    (13 reference docs)
```

---

## Layer 1 — The Data Source: Local Filesystem (Dealum deferred)

**As of plugin v2.12.0 (April 28, 2026): Dealum API integration is deferred.** The pipeline operates filesystem-first against `deals/active/<Company>/` folders. The Dealum MCP server is preserved in the plugin (dormant) for trivial restoration when the API is approved. See CLAUDE.md → "Dealum Integration Status — Deferred" for the full status and restoration procedure.

**Active state:**

**Source of truth:** `deals/active/<Company>/Data Room/` and `deals/active/<Company>/Reports/` — Claude reads these directly when running pipeline commands. Stage progression is reflected in the contents of `Reports/` (Triage Report → Scout Assessment → DD Kickoff Package → DD Report → Memo) rather than in a CRM step field.

**Pipeline-monitor / `/sync-pipeline`:** Should produce a snapshot from filesystem inspection (count/list deals by stage based on which Reports exist). The Dealum-calling implementation is preserved but dormant.

**Deferred state (when Dealum API is approved):**

The plugin still contains `servers/dealum_server.py` (8 KB Python script) that wraps the Dealum Integration API and would expose 5 tools — `list_applications`, `get_application`, `update_application`, `list_members`, `create_application`. To restore: re-add the `nwai-dealum` MCP server block to both `.mcp.json` files, install Python 3.10+ and the `mcp` package on Mac, and set `DEALUM_TOKEN` / `DEALUM_ROOM_ID`. The exact MCP server block is preserved in git history at any commit prior to v2.12.0.

**The analogy:** Today, the deal folders themselves are the filing cabinet — Claude reads the actual files. When Dealum integration activates, the cabinet gets a CRM index on top, and the MCP server becomes the key.

---

## Layer 2 — The Pipeline Stages

Every deal is a Dealum application with a **step** tag that tracks its position:

```
📥 Inbox  →  🔍 Screening  →  🤝 Scout/IntroCall  →  🔬 Diligence  →  📋 DD Report  →  ✅ Decision  →  📝 Memo
```

Claude reads the step field to know what depth of analysis is appropriate (see Output Depth in CLAUDE.md). Advancing a deal = calling `update_application` with the new step name.

---

## Layer 3 — Commands (The Eight Workflows)

Each command is a markdown file in `commands/` that defines a structured workflow Claude follows when you type the slash command.

| Command | File | What It Triggers | Output |
|---------|------|-----------------|--------|
| `/sync-pipeline` | `commands/sync-pipeline.md` | `pipeline-monitor` agent → Dealum snapshot | Pipeline dashboard in chat |
| `/screen [company]` | `commands/screen.md` | `gates-and-flags-techgroup.md` (self-contained Six-Signal screener v3.1: 3 hard gates + NWA Filter evidence rigor + six signal verdicts w/ confidence tags + conviction mechanics + 22-rule calibrated rules ledger, incl. Source-Tier Rule, Identity-Match Standard, Confidence-Gated Compound Rule, Moat Sufficiency Test) + `agent-era-readiness-framework.md` (Signal 5) + `alpha-ai-sovereignty-framework.md` (Signal 6). **No numeric scores** — Verdict Block leads: PRELIMINARY CALL + TRIAGE CONVICTION | 1–2 page Six-Signal Triage brief in chat + Triage Report .docx saved to deals/active/<Company>/Reports/ |
| `/scout [company]` | `commands/scout.md` | `nwai-investment-framework` skill → Scout Q assessment (Q1–Q8 incl. Agent-Era Q7 + Alpha-Sovereignty Q8) + theme map. Intake applies `screen-scout-conversion-rubric.md` mechanically — signal verdicts → D1–D5 (/25) + R1–R4 (/20) baseline for delta tracking + Thesis Fit (/45); ≥2 divergences from baseline require a one-line explanation. Step 4c: Founder Claims Reconciliation & Credibility Cap — any pass scored on founder-provided follow-up material must classify prior load-bearing claims CONFIRMED/QUALIFIED/WALKED BACK first; ≥2 walk-backs = ❌ credibility pattern (ADVANCE off the table); such a document cannot net-raise conviction; Reported-tier (self-reported) evidence cannot raise dimension scores | Scout Assessment report in chat + Scout Assessment .docx saved to deals/active/<Company>/Reports/ |
| `/diligence [company]` | `commands/diligence.md` | Pre-flight gate (Scout required + financial files confirmed) → 5–6 agent team → Layer 2 Hypothesis Confirmation Plan (AI-derived) + scored rubrics + 17-folder checklist | DD Kickoff Package .docx saved to deals/ |
| `/post-meeting [company] [product\|gtm\|financials\|interview]` | `commands/post-meeting.md` | Loads transcript(s) + all prior context → applies analyst lens → produces dual-output document. `interview` = customer interview (meeting with company's customers, not the company itself); applies four-question Behavioral-Intensity Question Bank. Claim walk-back lens (Decision 10 extension): load-bearing prior claims contradicted by the transcript classified CONFIRMED/QUALIFIED/WALKED BACK; cumulative credibility ledger across the diligence record — 1 walk-back = ⚠️ in Analyst POV box, ≥2 = ❌ credibility pattern leading the POV + pause-or-pass recommendation; Reported-tier statements cannot flip a 🔴 stress point to 🟢 | Post-Meeting Reconciliation .docx (Analyst POV + Key Insights + Resolved/Open tracker) saved to deals/active/<Company>/Reports/ |
| `/dd-report [company]` | `commands/dd-report.md` | `nwai-investment-framework` skill → scored synthesis of completed diligence | DD Investment Report .docx (11 scored sections, 2-column RAG table layout, IC-ready) saved to deals/ |
| `/decision [company] [verdict]` | `commands/decision.md` | `update_application` → Dealum updated with decision + tags | Decision recorded in Dealum |
| `/memo [company]` | `commands/memo.md` | `nwai-investment-framework` skill → Executive Summary deck (pptxgenjs from `scripts/node_modules`; visual QA via LibreOffice→PDF when installed, else flagged for manual review) | 4-slide PPTX saved to deals/active/<Company>/Reports/ |

**The analogy:** Commands are like work orders on the factory floor. You hand one to Claude, it knows exactly which tools and frameworks to pull for that station.

---

## Layer 4 — Agents (The Research Workers)

Agents are **autonomous subprocesses** Claude can launch to do research in parallel. Each is defined by a markdown file in `agents/` describing its purpose, data sources, and output format.

### `pipeline-monitor`
- **Trigger:** "What's in the pipeline?" / `/sync-pipeline` / session startup
- **Does:** Calls `list_applications` with tag_filter="Tech", groups by step, surfaces stalled deals and suggested next actions
- **Returns:** Pipeline dashboard with deal counts per stage

### `team-analyst`
- **Trigger:** `/scout` (full) + `/diligence` (full)
- **Does:** Web research on founders, funding history, traction signals, commercial validation (contract structure, named customers, sales motion), public red flags
- **Returns:** Structured company + founder + commercial briefing → feeds Team Validation + Commercial Validation (Layer 2)

### `market-analyst` ★ NEW
- **Trigger:** `/scout` (full) + `/diligence` (full)
- **Does:** Structural discontinuity test, independent TAM/SAM/SOM validation, market timing scoring, industry dynamics, customer behavior signals
- **Returns:** Market Analysis Briefing → feeds Market Validation (Layer 2)

### `competitive-positioning-analyst`
- **Trigger:** `/scout` (full) + `/diligence` (full)
- **Does:** Maps direct competitors and incumbent threats, alternative solutions, strategic incumbents, positioning analysis (incl. an explicit **BLUE/CONTESTED/RED ocean classification** feeding the Scout Blue Ocean Read), moat input data
- **Returns:** Competitive Positioning Analyst Briefing → feeds Competitive Validation (Layer 2)

### `technology-analyst` ★ NEW
- **Trigger:** `/scout` (light — thin wrapper + TRL) + `/diligence` (full)
- **Does:** Thin wrapper detection (3 tests) — including a **NON-LLM ALGORITHMIC ENGINE** branch for products whose core is proprietary math/algorithms (causal engines, optimizers, simulation, novel ML architectures) rather than LLM API calls; such engines are categorically NOT thin wrappers and shift the replicability burden to published-vs-novel algorithms — TRL assessment, IP/patent research, technical architecture signals, AI moat input data (incl. **inference-economics probing** + explicit **Training-edge / Inference-edge** PRESENT/ABSENT verdicts feeding the Two Sustainable Advantage Tests), technical team depth
- **Returns:** Technology Analyst Briefing → feeds Technical Validation (Layer 2)

### `risk-analyst` ★ NEW
- **Trigger:** `/scout` (light — top kill risks) + `/diligence` (full)
- **Does:** Regulatory risk research, exit landscape + acquirer dynamics, execution risk signals, market and financial risk signals from public sources
- **Returns:** Risk Assessment Briefing → feeds cross-cutting risk overlay across all Layer 2 groups

### `pricing-analyst` ★ NEW (v2.13.0)
- **Trigger:** `/diligence` Stage 2A (parallel)
- **Does:** Pricing maturity assessment (PROVEN / EARLY SIGNALS / DISCOVERY / UNKNOWN — most early-stage companies are still in pricing discovery), unit economics (CAC, LTV, payback), channel economics with margin compression forecast (third-party distributors typically command a meaningful markup multiple of the direct price; this gap compresses over time as the channel partner gains volume leverage), value proposition validation with quantified pricing-to-value ratio, pricing pressure forecast (commoditization risk)
- **Returns:** Pricing Analyst Briefing → feeds Forecasting Analyst revenue model + Venture Analyst valuation; feeds DD Report S5 (GTM)

### `forecasting-analyst` ★ NEW (v2.13.0 — replaces legacy `financial-analyst`)
- **Trigger:** `/diligence` Stage 2B (sequential, after pricing-analyst)
- **Does:** Builds independent 5-year financial forecast using the McMurry method (build the proprietary forecast first, then compare to the company's submitted numbers — never start from the company's spreadsheet). Enforces the "no AI slop" rule (critical lens, independent research, accountable analyst POV with mandatory *because* clauses on every Bear/Base/Bull scenario). Produces P&L + cash flow + balance sheet (the cash flow / balance sheet view is non-negotiable, not optional); 5-year capital plan with round timing/sizing; Founder Financial Literacy Assessment (does the entrepreneur understand their own business?)
- **Returns:** Forecasting Analyst Briefing → feeds Venture Analyst; feeds DD Report S9 (Financials); feeds Memo Slide 3 pro forma

### `venture-analyst` ★ NEW (v2.13.0)
- **Trigger:** `/diligence` Stage 2B (sequential, after forecasting-analyst — final layer)
- **Does:** Final synthesis of financial diligence. Produces defensible valuation today (3 methods reconciled: revenue multiple, forward multiple, discounted future value at the 35% IRR hurdle); projected exit valuation Y3 + Y5 across Low/Base/High scenarios; capital + dilution modeling; IRR / multiple; 35% IRR hurdle test; NWA 10x-in-5-years criterion check; deal structure recommendation (priced equity / convertible with cap / participating preferred / re-priced terms / decline). Closes the founder-claimed-vs-defensible-analysis gap with documented methodology.
- **Returns:** Venture Analyst Briefing → feeds DD Report S8 (Deal Structure), S9 (Financials), S11 (Exit); feeds /decision command; feeds Memo Slide 4 Returns and Deal Terms

**The analogy:** Agents are like junior analysts you dispatch to do legwork. At Scout, four agents do targeted research to build the assessment evidence base. At Diligence, six agents run in parallel as Stage 2A (the five standard agents plus pricing-analyst). Then Stage 2B runs the financial diligence chain sequentially: forecasting-analyst → venture-analyst, because each consumes the upstream output. They report back with structured briefings; Claude synthesizes them into Layer 2 conclusions.

---

## Layer 5 — The Investment Framework Skill

**Location:** `skills/nwai-investment-framework/`

This skill is the **brain trust** — NWAi's proprietary investment knowledge encoded as reference documents that Claude loads during analysis. Thirteen reference files:

| File | Contents | Used At |
|------|----------|---------|
| `gates-and-flags-techgroup.md` | **TechGroup Six-Signal Screener Reference v3.1** (self-contained, August 2026): 3 hard gates + gating rule, NWA Filter evidence-rigor tests (Cynical Default → confidence tags, Goliath + LLM Ingestion → Moat evidence tags, Revenue Quality → Commercial Proof), Track Determination (A/B), the Six Signals (Discontinuity · Market + Commercial Proof · Team · Moat + tags · Agent-Era Posture · Protect Alpha) with verdict scales + basis style + brevity caps, confidence tags w/ UNVERIFIED middle-verdict cap + Ordering requirement, conviction mechanics (weights: Team HIGHEST; adverse-at-bottom-tier definitions; compound rule + Confidence-Gated Compound Rule; Fix-Forward; Ask-at-Scout; traction lift), Between-Signal Reads w/ fixed 5-pair coherence diff-list, team-configuration rules w/ Source-Tier Rule + Identity-Match Standard, Moat Sufficiency Test, AI Wrapper Assessment + Replicability Speed Flag, **22-rule Calibrated Rules Ledger** (back-test adjudications), 3-search Research Protocol w/ fixed founder-identity query set, Live Pitch Questions, TRL reference. Judgment model proven in the Claude V13 back-test; rubric-fidelity fixes (v3.1) ported from the same sandbox (Decision 12). No numeric scores | Screen (TechGroup) |
| `gates-and-flags-universal-guide.md` | **NWAi Group Triage Framework — Universal Guide** (rewritten + renamed from `gates-and-flags.md`, July 2026): the high-level architecture every group playbook follows — hard gates / rigor filters / judgment model / ADVANCE-WATCH-DECLINE verdict vocabulary / bounded research discipline / output contract — with TechGroup as the reference implementation and a build-a-new-group-playbook procedure. No thresholds or rubrics | Guide (future group playbooks) |
| `screen-scout-conversion-rubric.md` ★ NEW | **Screen→Scout Conversion Rubric v1.0** (frozen July 2026): the fixed verdict→points translation applied mechanically at Scout intake — Opportunity D1–D5 (/25) + Readiness R1–R4 (/20); D6 + R5 intentionally unmapped (Thesis Fit stays /45); Signals 5/6 don't map (scored fresh at Q7/Q8); gates carry as gates; Unverified claimed revenue caps D5 at 3; explained-divergence rule (≥2 → one-line explanation). Calibration record: STL + Synergist anchors, ±2 per-dimension, CC excluded (scale mismatch) + spec-citation correction documented. Loaded by `/scout` only — the number never appears at Screen | Scout (intake) |
| `_archive/` | Superseded screeners, reference only: `gates-and-flags-universal-v2.0-legacy.md` (numeric Universal Triage Framework, Apr–Jul 2026), `gates-and-flags-techgroup-numeric-v2.1-legacy.md` (numeric Track A/B rubrics), `gates-and-flags-6gate-legacy.md` (pre-v2.0 AutoKill) | Reference only |
| `scout-questions.md` | Phase 1 viability (Q1 + Q1b Demand Signal Test + **Blue Ocean Read** synthesis node + Q2 + Q3 Moat incl. Training/Inference edge reads, scored 0–5 with mapped-baseline delta) + 5 strategic dimensions (Q4 Ecosystem Role, Q5 Adjacent Displacement Risk, Q6 Macro Tailwind, **Q7 Agent-Era Readiness**, **Q8 Alpha-AI Sovereignty** ★ NEW — three reads + conduit cap + KEEPS/MIXED/LEAKS member block, N/A if no model supply chain) + Phase 2 execution (scored 0–5) + Scout Conviction Score (/19, unchanged — Q7+Q8 added within the 20% strategic weight, raw /25) + **Signal-First Member Format v2 (July 2026)** — Analyst Verdict leads (One-Sentence Verdict + Single Biggest Risk + dual scores), six Screen-vocabulary signal blocks with score chips and dimension→home map (each dimension renders once), Execution & Path table, deal-team appendix (Triage carry-forward, 13-row Score Summary + deltas, arithmetic, sources) | Scout |
| `diligence-scoring-rubrics.md` | Moat score Tier 1 (0–6) / Tier 2 AI (0–10) / **Tier 3 Agent-Era Readiness (0–15, every deal)** / **Tier 4 Alpha-AI Sovereignty (0–15, gated to model-supply-chain deals; Alpha Containment · Model-Layer Independence · Customer Sovereignty Alignment; FORTIFIED/HEDGED/LEAKY/CAPTURED; agent assignments) ★ NEW**, Risk score (1–10, includes Adjacent Displacement Risk sub-dimension), Bear/Base/Bull financial model; Layer 2 rubric-to-validation-group mapping table | Diligence |
| `dd-checklist.md` | Layer 2 Hypothesis Confirmation architecture + Layer 1 17-folder data completeness checklist; includes Layer 2 → DD Report section score mapping | Diligence |
| `ai-moats-framework.md` | Two Sustainable Advantage Tests (Training edge + Inference edge, PRESENT/ABSENT front-door moat lens) + three moat types for evaluating AI company defensibility + Replicability Speed Matrix (4-row threat actor table with Yellow Flag triggers) | Screen + Scout + DD Report + Memo |
| `agent-era-readiness-framework.md` | **The third always-active investing lens.** Tests whether a startup solves the problem *as AI agents will reshape it*. Core Challenge (dissolving-problem paradox) + doorway question + three scored test dimensions (Problem Reimagination · Position in the Agent Economy · Agent-Era Moat, 0–5 each on dual horizons) + four-posture classification (Threatened / Riding / Enabling / Insulated, with mixed-posture path+trigger rule) + domain-transformation map (Work/firm + Commerce/markets primary) + headline verdict (REIMAGINING/ADAPTING/EXPOSED/BLIND) + flag triggers. Derived from `docs/strategy/future-of-venture-investing/NWAi-Agent-Era-Readiness-Thesis-2026-06.md`. | Screen (**Signal 5 weighted posture verdict** — upgraded July 2026 from the trip-wire) + Scout (Q7) + Diligence (Moat Tier 3) + DD Report + Memo |
| `alpha-ai-sovereignty-framework.md` ★ ACTIVE (Phase 2 installed July 2026) | **The fourth always-active investing lens.** Tests whether the company keeps its alpha — and its customers' — out of the frontier labs' hands. Supplier-Predator Problem + alpha-flow doorway question + three scored dimensions (Alpha Containment · Model-Layer Independence · Customer Sovereignty Alignment, 0–5 each, single horizon + direction-of-travel line) + four-posture classification (Leaking / Hedged / Sovereign / Enabler) + headline verdict (FORTIFIED/HEDGED/LEAKY/CAPTURED) + no-double-count interaction map + flag triggers/caps. Screen deviation on record: trip-wire design superseded by V13-evolved Signal 6 Protect Alpha. Derived from `docs/strategy/future-of-venture-investing/NWAi-Alpha-AI-Sovereignty-Thesis-2026-07.md`. | Screen (Signal 6, routing-only) + Scout (Q8) + Diligence (Moat Tier 4) + DD Report (S3 companion line, S4/S10 feeds, Appendix A standing item) + Memo |
| `dd-report-format-reference.md` | 11-section scored report structure, 1–5 scale mapping, exact STL visual layout (2-column table, 9432/1080 DXA, RAG hex colors), 5 Sharp & Succinct content rules, format drift incident log | DD Report |
| `memo-format-reference.md` | 4-slide PPTX structure, NWAi branding, ++/-- notation, "What is the Bet?" format | Memo |
| `diligence-analysis-framework.md` ★ NEW | Analyst lens behavioral standard for the 4 diligence deep-dive meeting types. Defines the dual role (analyst POV primary / tracker secondary), declaration taxonomy, meeting-type playbooks (Product / GTM / Financials / Customer Interview), progressive POV architecture, and post-meeting document output structure. Customer Interview playbook (v0.26.0) includes the four-question Behavioral-Intensity Question Bank: Tool replacement & adoption, Unplanned use cases, Organic evangelism, Counterfactual loss. | Diligence (post-meeting) |
| `citation-contract.md` ★ NEW | **Source-attribution standard for all agent-generated research.** Two-class rule (cite external facts with inline `[n]`; reason for analytic judgments — never citation-dress a verdict) + the cite-from-retrieval rule (a `[n]` may only point to a source actually opened in-session — fabricated citations are hallucination with a tie on) + confidence tiers (Verified / Reported / Inferred) + `Sources` output convention + command-assembly rules (merge agent SOURCES blocks, route the unverified set into the diligence questions). Enforced inline in all 5 research agents and at report assembly. | Screen + Scout + all 5 research agents |

**The analogy:** If commands are work orders and agents are junior analysts, the skill is the NWAi investment playbook sitting on every analyst's desk — the same criteria applied consistently, every time, regardless of how exciting the pitch sounds.

---

## Layer 6 — Session Context: CLAUDE.md

**File:** `CLAUDE.md` (workspace root)

This is the **persistent memory layer** — loaded at the start of every Cowork session. It tells Claude:

- Who Jamie is and what this workspace does
- NWAi's investing voice and philosophy (blunt verdicts, moat obsession, four always-active lenses)
- The full pipeline stage definitions
- Output depth calibration by stage
- Required Scout report elements
- Non-negotiable investment criteria
- TechGroup themes + SME table
- Behavior dos and don'ts

CLAUDE.md is what makes Claude behave like a trained NWAi analyst rather than a generic AI assistant. It's the difference between "here's a balanced overview of this startup" and "Kill — no moat, thin wrapper, SAFE structure."

---

## End-to-End Flow Example

```
You type:  /screen Acme AI

Claude:
  1. Loads gates-and-flags-techgroup.md (Six-Signal screener v3.1) + agent-era +
     alpha-sovereignty frameworks
  2. Gathers deal materials (deals/active/Acme AI/Data Room/ + Dealum when available)
  3. Assigns Track (A/B); runs the 3 mandatory searches SILENTLY (market · founder
     commitment w/ LinkedIn check · competitive/Goliath)
  4. Applies the 3 hard gates (FAIL only on clear evidence → Verdict Block + Context +
     Gates + footer, stop)
  5. Works the Six-Signal evidence (NWA Filter tests → evidence tags; between-signal
     reads: coherence pass, narrative-vs-substance, optional pattern note)
  6. Renders verdicts + confidence tags; applies conviction mechanics (weights, adverse
     definitions, compound rule, Fix-Forward, Ask-at-Scout, traction lift)
  7. Outputs the 1–2 page brief: Verdict Block (PRELIMINARY CALL + TRIAGE CONVICTION +
     roll-up) → Context → Gates → Six Signal blocks → Deal Facts → Risk Flags → footer
     — NO numeric scores anywhere
  8. Generates Triage Report .docx → deals/active/Acme AI/Reports/
  9. Records verdict + tags in the deal record (Dealum when available)

You type:  /scout Acme AI

Claude:
  1. Loads nwai-investment-framework skill (scout-questions.md + ai-moats-framework.md +
     agent-era + alpha-sovereignty frameworks)
  1b. ★ Loads the most recent Triage Report + screen-scout-conversion-rubric.md; translates
      the six signal verdicts MECHANICALLY into the per-dimension baseline (D1–D5 /25 +
      R1–R4 /20); carries forward gates, flags, Moat tags (→ Q3 evidence), Deal Facts,
      and the Signal 5/6 reads as priors for Q7/Q8
  2. Runs Phase 1 viability (delta vs. mapped baseline; ≥2 divergence → one-line explanation)
  3. Runs Strategic dimensions Q4–Q8 (Q7 Agent-Era + Q8 Alpha-Sovereignty scored fresh)
  4. Runs Phase 2 depth questions; computes Scout Conviction (/19) + Thesis Fit (/45 from
     the mapped baseline)
  5. Maps to TechGroup theme (Lead/SMEs: TBD — Pending Dealum API)
  6. Outputs Scout Assessment report (one-sentence verdict, biggest risk, 3–5 diligence
     questions, 13-row Score Summary)
  6b. Generates Scout Assessment Report .docx → deals/active/Acme AI/Reports/
  7. Advances the deal record to Scout/IntroCall

You type:  /diligence Acme AI

Claude:
  0. Pre-Flight Check: verifies Scout Assessment Report exists in workspace (hard gate —
     halts if not found); displays confirmation prompt requiring CONFIRM before proceeding
     (reminds user to verify financial files are in deal room)
  1. Fetches from Dealum
  1b. ★ Loads Scout Assessment Report (required) + Triage Report (if present); Scout
      thesis is the analytical backbone for Layer 2 hypothesis generation
  2. Stage 2A — launches 6 agents in parallel: team-analyst (PMTF + verification),
     market-analyst, competitive-positioning-analyst, technology-analyst, risk-analyst,
     pricing-analyst
  2b. Stage 2B — sequential financial diligence chain: forecasting-analyst (independent
     5-yr forecast via McMurry method) → venture-analyst (valuation, hurdle test,
     deal structure recommendation)
  3. Loads diligence-scoring-rubrics.md + dd-checklist.md; applies Moat, Risk, Financial,
     and Market sizing rubrics using all agent briefings
  4. Assembles DD Kickoff Package (6 parts):
     Part A: Deal Brief + Scout thesis carry-forward
     Part B: Layer 2 — Hypothesis Confirmation Plan (AI-derived — 6 validation groups,
             each with hypothesis from Scout, AI conclusion, 🟢/🟡/🔴 signal, biggest uncertainty)
     Part C: Scored Assessment (Moat Tier 1 + Tier 2, Risk, Market/Finance)
     Part D: Layer 1 — 17-Folder Data Completeness Checklist [Hub]
     Part E: Founder Questions (Scoring Inputs + Nice to Have)
     Part F: Recommended Next Steps + Kill Conditions table
  5. Saves .docx to workspace
  6. Advances Dealum step to Diligence

You type:  /dd-report Acme AI   ← after DD team completes the 17-folder work

Claude:
  1. Loads dd-report-format-reference.md + diligence-scoring-rubrics.md (+ ai-moats-framework.md
     if AI-enabled). Reads Synergist canonical reference if present in workspace.
  1b. ★ Checks deals/ for most recent Triage Report + Scout Assessment Report + DD Kickoff
      Package; loads all three and uses them as primary inputs to all 11 scored sections
  2. Derives 1–5 score for each of 11 sections from NWAi rubric outputs
  3. Applies 5 Sharp & Succinct content rules (Once and Down, Section Mandates Exclusive,
     Tables Absorb Facts, Risk Synthesizes, No Closing Restatements) before writing each section
  4. Generates scored Word doc: Recommendation banner → Company header → 11 scored sections
     (each as 2-column table: 9432 DXA content | 1080 DXA RAG-colored score) → Recommendation
     table → DD Team Votes → Appendix A (outstanding diligence items)
  5. Saves .docx to workspace
  6. Advances Dealum step to Decision, tags "DD-Report-Complete"

You type:  /memo Acme AI   ← after /decision records the IC vote

Claude:
  1. Loads memo-format-reference.md + ai-moats-framework.md
  1b. ★ Checks deals/ for most recent DD Report (primary); if not found, falls back to
      loading Kickoff Package + Scout Report + Triage Report individually
  2. Compiles all pipeline data + web research to fill 4-slide structure
  3. Generates 4-slide PPTX (navy/ice-blue NWAi branding) via pptxgenjs
  4. Runs visual QA on all slides
  5. Saves .pptx to workspace
  6. Tags Dealum "Memo-Complete"
```

---

## File Structure Summary

```
nwai-tech-pipeline/
├── .claude-plugin/
│   └── plugin.json          ← Plugin metadata (name, version, author)
├── .mcp.json                ← MCP server config (auth env vars injected here)
├── README.md                ← Plugin README
├── servers/
│   └── dealum_server.py     ← Python MCP server wrapping Dealum API (5 tools)
├── commands/
│   ├── sync-pipeline.md     ← /sync-pipeline workflow
│   ├── screen.md            ← /screen workflow
│   ├── scout.md             ← /scout workflow
│   ├── diligence.md         ← /diligence workflow (kickoff + 17-folder working doc)
│   ├── post-meeting.md      ← /post-meeting workflow (analyst lens + tracker reconciliation; 4 meeting types: product/gtm/financials/interview)
│   ├── dd-report.md         ← /dd-report workflow (scored synthesis, IC-ready)
│   ├── decision.md          ← /decision workflow
│   └── memo.md              ← /memo workflow (4-slide PPTX for members call)
├── agents/
│   ├── pipeline-monitor.md         ← Live Dealum pipeline snapshot agent
│   ├── team-analyst.md       ← Team-first PMTF + verification + commitment agent (v2.13.0 refresh)
│   ├── competitive-positioning-analyst.md ← Competitor mapping + moat input agent
│   ├── market-analyst.md           ← Discontinuity test + TAM/SAM + timing agent
│   ├── technology-analyst.md      ← Thin wrapper + TRL + IP + AI moat agent
│   ├── risk-analyst.md            ← Regulatory + exit + execution risk agent
│   ├── pricing-analyst.md          ← Pricing maturity + unit economics + channel pressure agent ★ NEW v2.13.0
│   ├── forecasting-analyst.md      ← Independent 5-yr forecast (McMurry method) agent ★ NEW v2.13.0 (replaces financial-analyst)
│   └── venture-analyst.md          ← Valuation + hurdle test + deal structure agent ★ NEW v2.13.0
└── skills/
    └── nwai-investment-framework/
        ├── SKILL.md         ← Skill entry point
        └── references/
            ├── gates-and-flags-techgroup.md   ← TechGroup Six-Signal Screener Reference v3.1 (self-contained)
            ├── gates-and-flags-universal-guide.md ← Group Triage Framework — Universal Guide (renamed from gates-and-flags.md)
            ├── screen-scout-conversion-rubric.md ← Frozen Screen→Scout verdict→points translation v1.0 ★ NEW
            ├── scout-questions.md             ← Q1–Q8 (Q8 Alpha-AI Sovereignty ★ NEW)
            ├── diligence-scoring-rubrics.md   ← Four-tier Moat (Tier 4 Alpha-AI Sovereignty ★ NEW) + Risk + Financial
            ├── dd-checklist.md
            ├── ai-moats-framework.md          ← + LLM-provider-row alpha-leakage annotation
            ├── agent-era-readiness-framework.md ← Third always-active lens (Screen Signal 5 / Scout Q7 / Diligence Tier 3)
            ├── alpha-ai-sovereignty-framework.md ← Fourth always-active lens ★ ACTIVE (Phase 2 installed July 2026)
            ├── dd-report-format-reference.md  ← + S3 Tier-4 companion line + Appendix A standing item
            ├── memo-format-reference.md
            ├── diligence-analysis-framework.md ← Analyst lens standard for post-meeting
            ├── citation-contract.md          ← Source-attribution standard (agents + Screen/Scout)
            └── _archive/
                ├── gates-and-flags-universal-v2.0-legacy.md    ← Numeric Universal Triage Framework (reference only)
                ├── gates-and-flags-techgroup-numeric-v2.1-legacy.md ← Numeric Track A/B rubrics (reference only)
                └── gates-and-flags-6gate-legacy.md  ← Pre-v2.0 legacy AutoKill (reference only)

Workspace root (canonical: /Users/jamie/ClaudeCodeProjects/nwa-intelligence):
├── CLAUDE.md                ← Persistent session context (the "brain")
├── nwai-techgroup-pipeline-architecture.md ← This file
├── pipeline-decisions-log.md  ← Running framework decision log
├── .claude/                 ← Pipeline runtime (mirror of plugin source — installed locally)
├── plugin/
│   ├── current/
│   │   ├── nwai-tech-pipeline.plugin  ← Latest installable plugin (v2.13.1)
│   │   └── nwai-tech-pipeline.zip     ← Org upload package (matches current plugin)
│   └── archive/             ← Prior versioned plugin files (rollback if needed)
├── scripts/
│   └── dd-report-generator.js  ← Canonical DD Report generator (MANDATORY — use for all /dd-report runs)
├── docs/                    ← Reference docs (committed to git)
│   ├── reference/           ← Plugin / platform reference documentation
│   │   ├── NWAi-TechGroup-Pipeline-Reference.{html,pdf}
│   │   ├── NWAi-TechGroup-Platform-Overview.{html,pdf}
│   │   └── agent-team-reference.md
│   ├── group-intake/        ← Per-group playbook intake questionnaires (May 2026)
│   │   ├── NWAi-Group-Playbook-Intake-Template.md
│   │   └── NWAi-Group-Playbook-Intake-TechGroup-Reference.md
│   ├── strategy/            ← Platform vision and strategic positioning (migrated from Desktop Apr 2026)
│   │   ├── NWAi-Grand-Unifying-Theory-v0.1.md
│   │   ├── NWA-AI-Investment-Intelligence-GUT.{html,pdf}
│   │   ├── NWAi-Strategic-Reframe-Reference.md
│   │   ├── NWAi-Huddle-Brief-2026-04-22.md
│   │   ├── NWAi-Spring-Member-Meeting-AI-Update.pptx
│   │   ├── Member_Social_Intelligence_Layer_V1_Features.md
│   │   ├── RaiseLink-NWAi-Brief-Comparison-2026-04.md
│   │   ├── NWAi-TechGroup-Managed-Agents-Architecture-v1.0.docx
│   │   ├── cowork-vs-enterprise-platform.md
│   │   └── Jamie & Ron_ The Grand Unifying Theory chat.md
│   └── build-history/       ← Plugin build session briefs (migrated from Desktop Apr 2026)
│       ├── Scoping.md
│       ├── Setup.md
│       ├── Session-Backlog.md
│       ├── Session-2-Brief.md
│       ├── Session-3-Brief.md
│       └── Desktop-Checkout-Reconciliation-Report.md
├── notes/                   ← Process notes and design memos (committed)
├── demo/                    ← Next.js companion demo artifact (committed; see Companion Artifact section)
└── deals/                   ← Local-only (gitignored — deal data never pushed to GitHub)
    ├── active/
    │   ├── Captain Compliance/
    │   │   ├── Data Room/
    │   │   └── Reports/
    │   ├── Summit Technology Laboratory/  (STL)
    │   │   ├── Data Room/
    │   │   └── Reports/
    │   └── Synergist Technology/
    │       ├── Data Room/
    │       └── Reports/
    ├── archive/             ← Closed deals
    └── _quarantine_pre_consolidation_2026-04-28/  ← Pre-consolidation duplicates pending manual cleanup

    Reports/ contains: Triage Report, Scout Assessment, DD Kickoff Package,
    DD Report, post-meeting reconciliations, Action Trackers, Investment Memo (.pptx).
    Each command loads the most recent prior-stage file by YYYY-MM-DD; multiple versions retained,
    only latest is loaded.
```

---

## Companion Artifact — NWAi Investment Intelligence Demo

**Location:** `demo/` (sibling to plugin source, same repo)
**Stack:** Next.js 14 App Router, Tailwind v4, shadcn/ui (base-nova), JSON seed data
**Purpose:** Information architecture and UX preview for the full NWAi Investment Intelligence Platform — the wider vision the plugin is one slice of.

The demo is a static, no-backend Next.js app that lets Jamie and TechGroup preview how the eventual platform will surface deal, member, and ecosystem intelligence in a unified workspace. It is **not** part of the plugin and ships independently. Slash commands, Dealum MCP, agents, and the framework skill all live in the plugin; the demo only mirrors their *outputs* against hand-seeded JSON so the IA, taxonomies, and cross-link surface area can be reviewed today without Dealum or any runtime LLM call.

### Top-level sections (as of Session 3, April 2026)

| Section | Route | What it shows |
|---------|-------|---------------|
| Pipeline | `/pipeline`, `/deals/[id]` | The 7-stage deal pipeline with stage cards reflecting Universal Triage v2.0 (Opportunity 6×0–5/30, Readiness 5×0–5/25, ADVANCE ≥ 20/30) |
| Members | `/members`, `/members/[id]` | Member directory with FL-city geography facet + collapsible filters (NWAi Group → Region → Expertise → Sector of Interest → Past Investment); member profiles with expertise, sectors, past investments, matched deals, contributed inputs |
| Matching | `/deals/[id]/matching` | Per-deal Matching Rationale — member scores, status, response excerpts |
| Portfolio | `/portfolio` | NWAi portfolio companies grid (10 seeded across all 6 NWAi groups) + slide-over drill-in (member investors, NWA + external board seats, primary contact, warm contacts) |
| Ecosystem | `/ecosystem` | Phase 4 placeholder feed for incubator/accelerator, university spinout, operator-network, and thematic-report sourcing — banner explicitly defers real signals to the future `ecosystem-scout` agent suite |
| Orchestrator | `/orchestrator` | Admin overlay — matching jobs, outreach queue, synthesis queue with progress bars and a 6-metric KPI strip |

A thin top-level **SectionNav** (Pipeline · Members · Portfolio · Ecosystem · Orchestrator) sits above the Pipeline-specific 7-stage StageNav; StageNav only renders inside the Pipeline section.

### Deployment

Local dev: `cd demo && npm run dev`. Public preview is via Vercel (free tier, **Root Directory = `demo/`**, auto-deploy from `main`). Demo carries no PII and no API keys — fictional members, fictional deals, all data is hand-seeded JSON in `demo/data/`.

### Relationship to plugin

The demo and the plugin share the same repo but are independently versioned. Plugin changes have their own changelog (this file) and `.plugin` packaging; the demo evolves through its own session-numbered build phases (Sessions 1–N). When the plugin gains new commands or surfaces, the demo's IA can choose to reflect them or not — the two are decoupled by design. The architecture file (this document) tracks plugin state; demo state is tracked in commit history under `demo/`.

---

---

## Change Log

| Version | Date | Change |
|---------|------|--------|
| v0.45.0 | August 26, 2026 | **Plugin v3.4.1 (patch) — Rubric Fidelity Amendment: Source-Tier Rule, Identity-Match Standard, Confidence-Gated Compound Rule, Moat Sufficiency Test.** Origin: a cross-session Claude Project screen of Constellation X (same inputs) produced one DECLINE and one ADVANCE, traced to a soft call on Team commitment evidence sitting directly on the compound rule; a follow-up live comparison test against this pipeline screener surfaced a second, independent divergence on the Moat WEAK/MODERATE boundary. Designed and validated in the Claude V13 sandbox first (`docs/reference/Gemini/`), then ported here. **`gates-and-flags-techgroup.md` v3.0 → v3.1:** (1) Source-Tier Rule + Identity-Match Standard added to Team-Configuration Rules (Signal 3) — Tier 2 aggregator/data-broker listings (RocketReach, Wiza, Aeroleads, ZoomInfo, Apollo, Crunchbase auto-pages) found in one search pass count as ONE source regardless of domain count, and name+city alone never confirms identity match; (2) Confidence-Gated Compound Rule — an UNVERIFIED-confidence adverse vote alone can't trigger a compound-rule DECLINE, downgrades to WATCH instead; (3) Moat Sufficiency Test (Signal 4) — MODERATE/STRONG requires naming an affirmative asset that survives the flagged Goliath(s); no nameable asset, or 2+ negative tags stacking, prints WEAK; (4) fixed 5-pair coherence diff-list replacing the open-ended coherence check; (5) fixed 4-query founder-identity search set (run in full, not stopped at first hit); (6) Calibrated Rules Ledger extended 18 → 22; (7) Confidence Tags section gains an Ordering requirement (assign the tag before the verdict, never back-filled). **`screen.md`** updated to match: Step 4 Search 2 fixed query set, Step 6 coherence checklist, Step 7 adverse-definitions cross-reference to the two new Signal 3/4 tests, Self-Check +4 lines. **Validation:** re-applying to Constellation X converges Team to MIXED/UNCONFIRMED and Moat to WEAK with a named reason, landing on WATCH — reproducible instead of session-dependent. **What did NOT change:** no signal weight, verdict-tier definition, gate, or compound-rule trigger structure. **Open risk flagged, not fixed:** Market (WEAK/MODERATE) and Agent-Era (THREATENED/RIDING) bottom-tier boundaries are equally undefined but have not yet produced a live divergence. **Incidental fix:** `scripts/repackage-plugin.sh`'s plugin.json description update used a sed substitution that produced invalid JSON whenever the existing description contained an embedded quote — had been silently corrupting `plugin.json` since at least v3.4.0; replaced with a JSON-aware Python update; file repaired and validated. **No MCP changes — reinstall not required.** Plugin repackaged to v3.4.1; v3.4.0 archived. See `pipeline-decisions-log.md` Decision 12. |
| v0.42.0 | July 15, 2026 | **Plugin v3.2.2 (patch) — `memo.md` path hygiene: last stale Cowork-era lookups removed; runtime now fully canonical.** Closes the item flagged in v0.41.0. Step 2b: `find /sessions .. Claude CoWork` WORKSPACE resolution replaced with canonical `deals/active/[Company]/Reports/` loading (archive fallback). Step 4: ad-hoc `npm install pptxgenjs` + cwd script replaced with the canonical pattern — generator script under `scripts/`, `pptxgenjs` resolved from `scripts/node_modules` (already installed), PPTX written directly to `deals/active/[Company]/Reports/[Company]-NWAi-Exec-Summary-[date].pptx`. Step 5 (Visual QA): Cowork `soffice.py` skill lookup replaced with a CLI-native LibreOffice check (PATH + `/Applications/LibreOffice.app`) → convert to PDF → Read-tool visual inspection; when LibreOffice is absent (current default on this Mac) QA is skipped explicitly and the Step 7 confirmation asks Jamie to eyeball the deck before member distribution. Step 6 reduced to the Dealum update (direct-write in Step 4 removes the copy leg; Dealum-deferred note added). Verified: zero `/sessions` / Cowork references remain anywhere in `.claude/` commands, agents, or skills. No format or framework changes. **No MCP changes — reinstall not required.** Plugin repackaged to v3.2.2; v3.2.1 archived. |
| v0.43.0 | July 16, 2026 | **Plugin v3.3.0 — Signal-First Scout Report (v2), Decision 11.** Scout Assessment restructured member-first on Jamie's review of the first post-Framework-Adoption Scout output (SecureG): Analyst Verdict leads (now carrying the required One-Sentence Verdict + Single Biggest Risk explicitly), the Six-Signal vocabulary carries from Screen into six Scout-depth signal blocks with score chips and a fixed dimension→home map (each scored dimension renders exactly once — kills the v1 orphan sections: Product & Market Positioning, Moat Assessment, Macro Trends, Adjacent & Emerging Tech, Phase 1/Strategic subsections), Execution & Path table (Traction/GTM/Technology-delivery/Exit; Technology deliberately split — defend-it evidence in Signal 4, ship-it risk here), Founder Claims Reconciliation retained post-signals with conditional promotion on a fired credibility cap, and a deal-team appendix absorbing the machinery (Triage carry-forward + mapped baseline, 13-row Score Summary with deltas and the explained-divergence rule, scoring arithmetic, Sources). Scoring mechanics unchanged (Q1–Q8, /19, /45, conversion rubric, Step 4c). Files: scout-questions.md format section, scout.md Steps 6/6b, CLAUDE.md Output Depth row. Validation: SecureG report regenerated in v2. **No MCP changes — reinstall not required.** Plugin repackaged to v3.3.0; v3.2.2 archived. |
| v0.44.0 | July 16, 2026 | **Plugin v3.4.0 — Scout Report v2.1 (Decision 11 addendum): analyst-voice rendering rules.** Binding synthesis-cell rule (judgment / anchor fact / consequence — insight first, ≤60 words); Six Signals rendered as a 3-column scanning table with Probe-next removed (probes route to Diligence Questions); Signal 3 Team leads with the highest structural signal; Execution & Path opens with a mandatory GTM Model & Consequences line; verdict-block scores relabeled plainly (Conviction / Criteria Fit) with the Dual-Score-Read row replaced by a conditional divergence note; Appendix B2 Supporting Reads absorbs TAM/SAM/SOM numbers, macro one-liners, moat two-tests, replicability timings, posture lines. Scoring mechanics unchanged. Files: scout-questions.md, scout.md. Validation: SecureG regenerated in v2.1. **No MCP changes — reinstall not required.** Plugin repackaged to v3.4.0; v3.3.0 archived. |
| v0.41.0 | July 15, 2026 | **Plugin v3.2.1 (patch) — `post-meeting.md` path hygiene: stale Cowork-era lookups removed.** Completes for `/post-meeting` the sweep v3.0.0 ran across screen/scout/diligence/dd-report (post-meeting was missed). Step 1: `find /sessions .. Claude CoWork` WORKSPACE resolution replaced with canonical `deals/active/[Company]/Reports/` loading (archive fallback for reopened deals; prior-reconciliation glob now also feeds the Decision 10 cumulative walk-back count). Step 2: `/sessions/*/mnt/uploads` transcript lookup removed — transcripts load from `deals/active/[Company]/Data Room/` or session-provided content. Step 7: Cowork docx-skill lookup replaced with the canonical Node.js + `docx` (`scripts/node_modules`) generation instruction; save path corrected from flat `deals/active/` to `deals/active/[Company]/Reports/` (filename convention unchanged — dd-report globs and existing on-disk reconciliation docs unaffected). No behavior, framework, or format changes. **Known remaining:** `memo.md` still carries 3 Cowork-era `/sessions` lookups (incl. the PPTX `soffice.py` skill path) — deferred; needs a CLI-native PPTX generation decision, flagged to Jamie. **No MCP changes — reinstall not required.** Plugin repackaged to v3.2.1; v3.2.0 archived. |
| v0.40.0 | July 15, 2026 | **Plugin v3.2.0 — Decision 10 extension: claim walk-back lens at `/post-meeting`.** Extends the Founder Claims Reconciliation & Credibility Cap (v3.1.0, Scout Step 4c) to the Diligence-stage meeting analysis, ratified by Jamie same day — meeting transcripts can walk back load-bearing claims exactly as the RootCause.ai DD Response did, and the post-meeting framework watched for "structural contradictions" but had no classification, no pattern escalation, and no cap. **Changes:** (1) `post-meeting.md` analyst lens — new "Claim walk-backs & the credibility pattern" block: prior load-bearing claims contradicted by the transcript classified CONFIRMED/QUALIFIED/WALKED BACK, each walk-back a Key Insight; (2) **cumulative credibility ledger** — the count accumulates across the whole diligence record (Scout Step 4c table + prior post-meeting docs), 1 = ⚠️ in the Analyst POV box, ≥2 = ❌ founder-credibility pattern that leads the POV box + running thesis update with a pause-or-pass recommendation until independently re-verified; new mandatory "Claims Reconciliation read" in the Step 6 POV-box classifications and a "Credibility ledger" line in the Step 8 running thesis update; (3) **symmetry rule** — Reported-tier (self-reported) statements may resolve tracker items but cannot flip a 🔴 thesis stress point to 🟢 without independent verification; (4) `diligence-analysis-framework.md` §2 (Structural Contradictions) extended with the same rule; (5) CLAUDE.md dual-role section gains the "Claim walk-backs" lens bullet. Dual-output document structure unchanged (ledger lives inside the existing POV box). **No MCP changes — reinstall not required.** Plugin repackaged to v3.2.0; v3.1.0 archived. See `pipeline-decisions-log.md` Decision 10 (Extension). |
| v0.39.0 | July 15, 2026 | **Plugin v3.1.0 — Founder Claims Reconciliation & Credibility Cap at Scout (the RootCause.ai lesson).** Trigger: RootCause.ai PASS recorded July 15, 2026 on TechGroup member (SME) review of the company's June 2026 Investor Diligence Response — the response walked back three load-bearing deck claims (production engine is well-known pivoted Cholesky, not the deck's tensor-train breakthrough framing, which is research-roadmap; DHL "$20M savings" is an unbooked estimate; quantified ROI is largely guesstimates the company disclaims owning), yet the June 16 Scout update — which flagged every discrepancy individually — still net-upgraded conviction 11.8→12.9 from *self-reported* logos and channel names in the same letter. Diagnosis: items-vs-pattern failure (no Scout equivalent of Screen's coherence cap) + asymmetric evidence treatment (founder's favorable self-reported claims raised scores immediately while unfavorable admissions were deferred to diligence). **Change: NEW `scout.md` Step 4c** — for any Scout scoring pass using founder-provided follow-up material: (1) reconcile every load-bearing prior claim CONFIRMED/QUALIFIED/WALKED BACK as a report table before re-scoring; (2) 1 walk-back = ⚠️ in the Analyst Verdict Block, ≥2 = ❌ founder-credibility red flag with ADVANCE off the table until independently re-verified (default DECLINE or triggered WATCH); (3) Credibility Cap — a walk-back document cannot produce a net conviction increase; (4) Reported-tier evidence cannot raise dimension scores (enforces the Citation Contract's tiers in the scoring mechanics). Scout rubrics, weights, Conviction /19, and all other stages unchanged. Deal artifacts: Decision Record + claims-reconciliation table at `deals/archive/RootCause.ai/Reports/RootCause.ai - Decision Record 2026-07-15.docx`; deal folder archived. **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v3.1.0 via `scripts/repackage-plugin.sh`; v3.0.0 binary archived to `plugin/archive/`. See `pipeline-decisions-log.md` Decision 10. |
| v0.38.0 | July 13, 2026 | **Plugin v3.0.0 (MAJOR) — Framework Adoption install: the Six-Signal Screen.** Executes `notes/NWAi-Pipeline-Framework-Adoption-Spec-2026-07.md` (ratified July 11, 2026) — the Phase 5 install anticipated by the Claude V13 sandbox program (`docs/reference/Gemini/`). Stage-architecture change, hence the major version. **(1) Screen rebuilt (Workstream A/D):** `screen.md` rewritten around the V13 judgment core — Six-Signal verdict model (Discontinuity · Market + Commercial Proof · Team · Moat + evidence tags · Agent-Era Posture · Protect Alpha), confidence tags with the UNVERIFIED middle-verdict cap, weighted conviction mechanics (Team HIGHEST; adverse only at bottom tier; compound rule; silent Fix-Forward; Ask-at-Scout; traction lift), Between-Signal Reads, V13 output structure (Verdict Block leads; signal blocks never tables; 1–2 page brevity contract), 28-item silent self-check, Craft-thesis traceability pointer. **No numeric scores at Screen** (Craft-at-Screen boundary — Decision 9). Agent-Era upgraded from Exposure trip-wire to weighted Signal 5 posture verdict (THREATENED = adverse). Docx redesigned to mirror the brief (signal blocks, no score tables); stale Cowork-era `find /sessions` path lookups removed across screen/scout/diligence/dd-report (canonical `deals/active/<Company>/Reports/` paths); kill-tag vocabulary updated (`Decline-CompoundAdverse`, `Decline-FixForward`). **(2) Reference layer:** `gates-and-flags-techgroup.md` restructured in place to the self-contained Six-Signal Screener Reference v3.0 (absorbs the hard gates, NWA Filter, research protocol, and TRL table; adds the 18-rule Calibrated Rules Ledger; numeric v2.1 archived). `gates-and-flags.md` **rewritten and renamed** `gates-and-flags-universal-guide.md` (Jamie's July 12 amendment to spec Decision 5 — its content served only TechGroup; now a 1–2 page architecture guide for future group playbooks; v2.0 archived). **(3) Screen→Scout seam (Workstream B):** NEW `references/screen-scout-conversion-rubric.md` v1.0 — fixed verdict→points translation applied mechanically at Scout intake (D1–D5 /25 + R1–R4 /20 per the ratified drop-D6+R5 subset; Thesis Fit stays /45); calibrated on STL + Synergist per-dimension within ±2 (CC excluded — scale mismatch; spec-citation correction recorded); explained-divergence rule (≥2 → one-line explanation) added to `scout.md`. **(4) Alpha-AI Sovereignty Phase 2 installed (Workstream C)** per the framework's own 12-item checklist, all 5 calibration questions ratified: Signal 6 Protect Alpha at Screen (V13-evolved trip-wire, routing-only), Scout **Q8** (Strategic group → 5 dims, same 20%/3.0, Conviction /19 unchanged; conduit cap 3/5 roadmapped / 2/5 unaware), Diligence **Moat Tier 4** (0–15, gated to model-supply-chain deals), DD Report S3 companion line + S4/S10 feeds + standing Appendix A provider-terms item (all AI deals), scoped sections in technology/risk/competitive-positioning analysts, Replicability Matrix LLM-provider-row **annotation** (never a fifth row), DRAFT banner removed (framework ACTIVE), fourth-lens entries in CLAUDE.md + SKILL.md. **(5) Proof plan passed before commit:** 18-rule traceability check ✅; Screen regression — back-test three re-screened blind from deal folders: Captain Compliance **ADVANCE/MEDIUM ✅**, STL **ADVANCE/MEDIUM ✅**, Synergist **ADVANCE/MEDIUM ✅**, zero verdict flips (Krew waived); one explained non-verdict divergence recorded (Synergist Protect Alpha KEEPS vs V13 MIXED — regression verified the CDW no-train clauses in-folder, evidence V13 never saw); rubric calibration frozen; end-to-end seam test (CC Screen→Scout) ✅ — surfaced and fixed one rubric gap (Unverified claimed revenue caps D5 at 3). **Scout-onward numeric scoring, DD Report format, Decision/Memo, and the other five groups: unchanged.** **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v3.0.0 via `scripts/repackage-plugin.sh`; v2.23.0 binary archived to `plugin/archive/`. See `pipeline-decisions-log.md` Decision 9. |
| v0.37.0 | June 23, 2026 | **Plugin v2.23.0 — TechGroup investing themes evolved to agent-era framing.** The five theme definitions in `SKILL.md` (and the CLAUDE.md themes table) rewritten to the agent-era thesis language (AI Infrastructure & Agent-Era Backbone · SW Enabled HW, Physical AI & Robotics · WorkTech & Vertical AI OS · Data Sovereignty, Security & AI Trust · Agentic Systems & AI Ops). *(Row backfilled July 13, 2026 — the June 23 session bumped the version without adding its changelog row; see commit `adc33c4`.)* |
| v0.36.0 | June 10, 2026 | **Plugin v2.22.0 — Citation Contract: source attribution for all agent-generated research.** Originated from TechGroup member feedback on the Causal.ai Scout report — a reader wanted to validate a claim about "causal-inference roles" and had no way to trace where the number came from. Adds a binding source-attribution standard so every external fact in a research briefing or assembled report is traceable to a source the agent actually retrieved. **New reference doc** `references/citation-contract.md`: (a) the **two-class rule** — cite external facts with an inline `[n]` marker; *reason* for analytic judgments (verdicts, scores, posture reads) and never citation-dress a judgment, since a judgment dressed as a sourced fact is worse than an honest one; (b) the **cite-from-retrieval rule** (non-negotiable) — a `[n]` may only point to a source actually opened in-session (a real WebSearch/WebFetch result with URL, or a provided deck slide # / transcript timestamp); fabricated or back-filled citations are "a hallucination with a tie on" and manufacture false confidence; (c) **confidence tiers** — Verified / Reported / Inferred; (d) the `Sources` **output convention**; (e) **command-assembly rules** — merge agent SOURCES blocks, dedupe/renumber, and **route the unverified load-bearing claims into the Targeted Diligence / Live Pitch Questions** (the highest-value downstream use). **Wiring (synced in `.claude/` and `plugin/unpacked/`):** (1–5) a "Citation Contract" runtime block added to all five research agents (`market-analyst`, `team-analyst` — extending its existing Founder Claim Verification Protocol, `competitive-positioning-analyst`, `technology-analyst`, `risk-analyst`) plus a `── SOURCES ──` block in each agent's output format. (6) `scout.md` — citation-contract assembly instruction in Step 6, a `Sources` element added to the Page-2 in-chat format and the Step-6b Word-doc structure, an unverified-claim routing clause on the Targeted Diligence Questions, and a SOURCES-preservation note in Step 1c. (7) `screen.md` — citation-contract instruction in Step 8 with a new `SECTION 7 — SOURCES` in the Triage Report box and the Step-9 Word-doc structure (compact; on a hard-gate FAIL, only the failing finding's source is listed). **No scoring math, gates, or rubric changed — purely an attribution/traceability layer.** **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v2.22.0 via `scripts/repackage-plugin.sh` (diff-guard passed; `plugin/unpacked/` regenerated from `.claude/`); v2.21.0 binary archived to `plugin/archive/`. Proposal that seeded this: `notes/proposal-report-citations.md`. |
| v0.35.0 | June 9, 2026 | **Plugin v2.21.0 — SAFE & lead-investor calibration: from penalized signal to neutral negotiation item.** Decision 1 (March 2026) moved SAFE structure and missing-lead-investor *off* the hard gates, but only changed the *timing* of the conversation — they remained scored **penalties** (SAFE = 2/5 Deal Structure; no-lead = 2/5 and "seeking NWA to lead" = 0/5 Syndication), and via the Readiness Downgrade Rule a SAFE + no-lead −6 hit could single-handedly demote a strong deal (Opportunity ≥ 20) to WATCH. With founders increasingly *starting* on a SAFE (open to negotiating) and NWA increasingly willing to lead/cross-syndicate, that treatment was both punitive and factually stale. **Five changes (synced in `.claude/` and `plugin/unpacked/`):** (1) `gates-and-flags.md` Readiness **Dim 1 (Deal Structure)** rescored — priced equity = 5 (bonus), SAFE/convertible on standard terms = **3 (neutral, normal starting point)**, below-neutral reserved for aggressive terms / founder inflexibility / incompatible structure; "deal-breaker" language removed. (2) **Dim 3 (Syndication Readiness)** reframed from "is there a lead?" to "is the round real and forming?" — external lead/co-lead = 5 (validation bonus), **NWA leading/cross-syndicating = 3 (neutral)**, 0/5-for-"seeking-NWA-to-lead" rule deleted, "NWA rarely leads" removed. (3) **Readiness Downgrade Rule carve-out** (the key mechanical fix) — Deal Structure & Syndication cannot downgrade an ADVANCE on their own; a downgrade now requires friction in Product Maturity, Traction Velocity, or Founder Accessibility. (4) `CLAUDE.md` Investment Criteria — Structure & Syndication relabeled as negotiated close-stage outcomes, not screen gates (NWA's eventual priced-equity preference unchanged). (5) Downstream **thread scrub** — the carry-forward "deal structure concerns" framing in `scout.md`, `diligence.md`, `dd-report.md` reframed as "negotiation items, not flags or kills"; venture-analyst's OPEN-item valuation-downweight clause carves out SAFE-presence/no-lead (only founder inflexibility or a dirty cap table weighs down); `SKILL.md` aligned. **No hard gates, Opportunity rubric, or scoring math changed.** **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v2.21.0 via `scripts/repackage-plugin.sh` (diff-guard passed; `plugin/unpacked/` regenerated from `.claude/`); v2.20.0 binary archived to `plugin/archive/`. See `pipeline-decisions-log.md` Decision 8. |
| v0.34.0 | June 9, 2026 | **Plugin v2.20.0 — Producer-side wiring for the new moat reads (v2.18.0 + v2.19.0 follow-through).** A downstream-consistency pass after the Two Sustainable Advantage Tests (v2.18.0) and the Blue Ocean Read (v2.19.0): the *framework* asked for these reads at Screen/Scout, but the research agents that produce the evidence didn't yet emit them — and a label collision existed at Diligence. **No breakage was found** (the new terms were self-contained in the three v2.18/v2.19 reference docs); this is purely the producer-side + carry-forward wiring. **Three changes (synced in `.claude/` and `plugin/unpacked/`):** (1) `technology-analyst.md` — the genuine gap: the agent probed the *training* side well (proprietary dataset / data flywheel / fine-tuning) but **never probed inference economics**, so the new Scout "Inference edge" read had no upstream evidence producer. Added an "Inference economics (the data-at-runtime test)" research block to Section 5 (distillation / self-hosting / cost-per-query / latency / edge-inference search) + two output lines in the AI MOAT SIGNALS block (`Sustainable Training edge` and `Sustainable Inference edge` PRESENT/ABSENT — mapping 1:1 to Scout Q3). (2) `competitive-positioning-analyst.md` — added an explicit **Ocean classification** (BLUE/CONTESTED/RED + closest-rival + moat-or-feature differentiation verdict) to Section 4 and the POSITIONING output block, so the agent *hands* Scout the Blue Ocean answer rather than leaving it to be assembled from competitor lists. (3) `diligence-scoring-rubrics.md` Tier 2 — a **cross-stage bridge note** reconciling vocabulary: surfaced that the existing "Inference Data Moat" row is actually an *operational data flywheel* (a training-side, data-in signal), **not** inference economics — a latent label collision with the new framework. The note maps Training Advantage → Training Data Moat + Custom model training, and routes true Inference Advantage (distillation/serving/cost) through Proprietary hardware/infrastructure + the carried-forward Scout read, treating ABSENT (full API pass-through) as margin-compression risk in the financial model. The "Inference Data Moat" label was deliberately **kept** (renaming risked breaking DD-report references) and explained instead. **No scoring math touched** (Tier 2 stays 0–10). **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v2.20.0 via `scripts/repackage-plugin.sh` (diff-guard passed; `plugin/unpacked/` regenerated from `.claude/`); v2.19.0 binary archived to `plugin/archive/`. |
| v0.33.0 | June 9, 2026 | **Plugin v2.19.0 — The Blue Ocean Read: market-creation classification at Screen + Scout.** Consolidates a framing raised in team feedback — *"first to market, or first to define a market? if not Blue Ocean, what's the adjacent set and the differentiation claim?"* — into a single explicit decision node. The substance was already distributed across Scout Q1 (category creation), Q5 (functional equivalents / displacement), and Track A D6 (Red Ocean economics); this bundles it so an analyst no longer has to assemble the read from three dimensions. **Design decision:** added as a named qualitative read (BLUE / CONTESTED / RED), NOT a scored dimension — scoring math at both stages is untouched; and the framework explicitly states *"Blue Ocean is not automatically better — a defended position in a Red Ocean beats an undefended Blue Ocean,"* keeping novelty subordinate to defensibility per NWAi's moat-obsession voice. **Scope:** Part A of the team's #2 feedback only — the Blue Ocean *framing*. Parts B/C (an internal "have we seen this before?" precedent scan + a decision-index seam) were deliberately deferred: the literal "Dealum scan" is blocked on the deferred Dealum API, and the local corpus is currently 4 deals — too thin to justify scan machinery. **Wiring (synced in `.claude/` and `plugin/unpacked/`):** (1) `gates-and-flags-techgroup.md` Track A Dimension 1 — one-line Blue Ocean read (BLUE/CONTESTED/RED + forced closest-competitor + differentiation claim, moat-or-feature verdict when not BLUE), applies to both tracks via the shared D1 rubric. (2) `scout-questions.md` — new non-scored "Blue Ocean Read" synthesis node after Q1b bundling Q1 market-creation + Q5 substitution (BLUE → name the category + demand-pull cross-check; CONTESTED/RED → required adjacent set + differentiation claim); plus two output hooks: a `Blue Ocean:` line in the Page 1 Analyst Verdict Block and an "Ocean type & differentiation" line in the Page 2 Adjacent & Emerging Tech cluster. **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v2.19.0 via `scripts/repackage-plugin.sh` (diff-guard passed; `plugin/unpacked/` regenerated from `.claude/`); v2.18.0 binary archived to `plugin/archive/`. |
| v0.32.0 | June 9, 2026 | **Plugin v2.18.0 — The Two Sustainable Advantage Tests: a front-door moat lens.** Elevates "sustainable training advantage" and "sustainable inference advantage" (a data-focused framing raised in team feedback) to the headline test every AI deal must answer *before* moat-type classification — the three existing moats become the *mechanisms*, these two become the *verdict*. **Design decision:** added as a named qualitative test (blunt binary PRESENT/ABSENT), NOT as new scored dimensions — so neither the Track A 30-pt Opportunity rubric nor the Scout /19 conviction composite is renormalized; scoring math is untouched. **Wiring (synced in `.claude/` and `plugin/unpacked/`):** (1) `ai-moats-framework.md` — new "The Two Sustainable Advantage Tests (Front Door)" section before the three moats (Test 1 Training Advantage = the data-in question: proprietary flywheel / exclusive data / human-feedback loop; Test 2 Inference Advantage = the data-at-runtime question, *the one most often skipped*: distillation / proprietary serving / cost-per-outcome edge that widens with volume), with a map back to the three moats; plus a required step-7 Two-Test Verdict in the scoring protocol (STRONG moat requires ≥1 test PRESENT; ABSENT on both = WEAK/NONE). (2) `scout-questions.md` Q3 Moat — two required reads (Training edge / Inference edge PRESENT/ABSENT) before the 4-column verdict; "Inference economics" added to the Primary Moat enumeration. (3) `gates-and-flags-techgroup.md` Track A Dimension 4 Defensibility — one summary "Two-test read" line that *reuses the already-required LLM Ingestion + Thin Wrapper outputs* (no new research, negligible screener burden); ABSENT-on-both is corroborating evidence for the thin-wrapper cap. **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v2.18.0 via `scripts/repackage-plugin.sh` (diff-guard passed; `plugin/unpacked/` regenerated from `.claude/`); v2.17.1 binary archived to `plugin/archive/`. |
| v0.31.0 | June 8, 2026 | **Plugin v2.17.0 — Agent-Era Readiness Framework: the third always-active investing lens.** Adds a new evaluation framework testing whether a startup solves the problem *as AI agents will reshape it* — across work, the firm, commerce, and every domain where agents become the predominant actor and consumer. Spawned from a thesis (`docs/strategy/future-of-venture-investing/NWAi-Agent-Era-Readiness-Thesis-2026-06.md`) synthesizing Cloudflare's bot-traffic crossover, Boris Cherny (post-coding firm transformation), Andrej Karpathy (decade of agents / autonomy slider), OpenClaw (grassroots agent adoption), and The Economist / Bret Taylor (agents reshaping global business). **Core reframe:** not "built for humans or for agents" (an interface question) but whether agents *reimagine, dissolve, or transform the problem itself* — a beautiful solution to a dissolving problem is the most dangerous bet. **New reference doc** `references/agent-era-readiness-framework.md`: doorway question (*"information for a human to act on, or a transaction for an agent to complete?"*) + three scored test dimensions (Problem Reimagination · Position in the Agent Economy · Agent-Era Moat, 0–5 each on dual horizons) + four-posture classification (Threatened / Riding / Enabling / Insulated, with mixed-posture path+trigger rule, validated against the Captain Compliance dry-run) + domain-transformation map + headline verdict (REIMAGINING/ADAPTING/EXPOSED/BLIND). **Wiring (synced in `.claude/` and `plugin/unpacked/`):** (1) `CLAUDE.md` — "Two lenses always active" → three (Structural Discontinuity + Memory Lock-in + Agent-Era Readiness). (2) `gates-and-flags-techgroup.md` v2.1 + `screen.md` — lightweight Agent-Era Exposure Signal (qualitative triage trip-wire, no score cap; adds an "Agent-Era Exposure: FLAGGED/CLEAR" row to the NWA Filter output). (3) `scout-questions.md` + `scout.md` — new Q7 Agent-Era Readiness strategic dimension (0–5 + posture tag); strategic group now 4 dims within the same 20% weight, Scout Conviction Score total /19 unchanged. (4) `diligence-scoring-rubrics.md` + `diligence.md` — Moat Tier 3 Agent-Era Readiness (0–15, runs for every deal, no AI gate; distinct from Tier 2 which tests build-replicability — Tier 3 tests whether the problem survives the shift); added to Part C scored output and Layer 2 mapping (Competitive primary, Market secondary). (5) `market-analyst.md` — agent-era discontinuity check + output block. (6) `technology-analyst.md` — agent-era moat signals (ingestibility, flywheel-under-load, transaction-vs-seat value capture, un-scrapable anchor, architecture flip) + output block. (7) `SKILL.md` v0.2.0 — registered as always-active lens. **Member-facing language pass (v2.17.1):** the four posture labels (Threatened/Riding/Enabling/Insulated) are now explicitly an *internal deal-team shorthand*; all member-facing outputs (Scout Q7, Diligence Tier 3, framework Output Format) lead with the plain question *"does the agent wave help or hurt this company?"* (HELPS/HURTS/MIXED) plus **"Could get stronger if:"** and **"What to watch:"** — replacing the insider "path + trigger" / bare-label phrasing. Scores and labels move to an internal "deal-team line." **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin repackaged to v2.17.1 via `scripts/repackage-plugin.sh` (diff-guard passed; `plugin/unpacked/` regenerated from `.claude/`); v2.16.0 binary archived to `plugin/archive/`. |
| v0.30.0 | June 2, 2026 | **Plugin v2.16.0 — technology-analyst: NON-LLM ALGORITHMIC ENGINE architecture branch.** Surfaced during the RootCause.ai Scout (a causal-discovery engine), where the technology-analyst correctly returned NOT A THIN WRAPPER but noted the engine "doesn't fit the standard AI-infrastructure taxonomy (CUSTOM MODELS / FINE-TUNED / RAG / PURE API) — it's a statistical/algorithmic causal engine, not an LLM system." The thin-wrapper test is fundamentally a test for *LLM API dependency*; products whose core differentiator is proprietary math/algorithms (causal-discovery engines, combinatorial optimizers, simulation/physics engines, novel ML architectures) make no LLM call and so were being shoehorned into a taxonomy built for LLM systems. **Three edits to `technology-analyst.md` (synced in both `.claude/agents/` and `plugin/unpacked/nwai-tech-pipeline/agents/`):** (1) Section 1 (Thin Wrapper Detection) — new lead-in block "Non-LLM algorithmic engine — check first" instructing the agent to label such a product **NON-LLM ALGORITHMIC ENGINE**, return Thin Wrapper Verdict = NOT A THIN WRAPPER, and shift the analytical burden to *algorithm replicability* (published vs. genuinely novel, open-source primitives vs. proprietary integration) rather than LLM-provider dependency; explicitly notes that an LLM used only as a thin UI/orchestration layer on an algorithmic core does NOT make it a wrapper. (2) Section 4 (Technical Architecture Signals) — `non-LLM algorithmic engine` added as first option in the AI/ML architecture taxonomy. (3) Briefing output format — `NON-LLM ALGORITHMIC ENGINE` added as first AI-infrastructure value, with a conditional instruction to express replication risk in algorithmic terms. **No MCP changes — reinstall not required; workspace `.claude/` is live immediately.** Plugin binary repackage to v2.16.0 + archive of v2.15.0 deferred to next end-of-session sync. |
| v0.29.0 | May 29, 2026 | **Plugin v2.15.0 — team-analyst expansion: mode-aware, bifurcated fit lenses, Staff + Network Map + Key-Seat checklist.** Substantial expansion of the team-analyst agent driven by NW investor feedback that team diligence ultimately looks at "Product-Team fit AND Market-Team fit, across founders, staff, AND advisors — including education, prior employment, skills onboard, market access, relationships, and team completeness (committed sales, CTO, etc.)." Five changes shipped as a unit. **(1) Mode-aware operation** — agent now reads a `mode: scout \| diligence` parameter from its invocation; Scout mode runs lite (one-line fit verdicts, no Staff Deep-Dive, no Network Map) for intro-call-ready depth; Diligence mode runs full underwriting depth. Section-by-section scope table baked into agent prompt. Defaults to scout if mode is omitted. **(2) PMTF bifurcated into Product-Team Fit + Market-Team Fit** — single composite "PMTF" replaced by two separate scoring lenses. Product-Team Fit asks "can this team BUILD and SHIP this product?" (engineering depth, technical credibility, prior shipping evidence). Market-Team Fit asks "can this team SELL into this market?" (domain immersion, sales motion fit, market-specific credibility, relationships). A team can score high on one and low on the other (great PhDs who can build but never met a buyer); the bifurcated reads surface that asymmetry. Composite "Team PMTF" preserved as a derived headline (lower of the two) for backwards compatibility with existing DD Report and memo references. **(3) Staff Deep-Dive (new Section 2f — Diligence mode only)** — non-founder VP / Head-of / C-level staff hires now get founder-grade research treatment (background, education, prior employment, market relevance, product relevance, relationships brought in from prior employer, claim verification). Closes the gap where staff were previously counted in Commitment Depth but not researched. **(4) Network Map (new sub-section under Market-Team Fit — Diligence mode only)** — explicit four-question structure: named-customer reachability, channel/partner relationships, KOL/regulator/standards-body access, prior-employer customer pipeline. Each connection output with explicit `Source:` field (today: Founder LinkedIn) — **structured as an extensible table** so the future NWA Member Social Graph Bonus (gated on Dealum API + NWA Member Social Intelligence Layer, currently deferred) can append `Source: NWA Member Graph (via [Member Name])` rows without restructure. Future-enhancement placeholder block inlined in Section 2c. **(5) Key-Seat Completeness Checklist (new Section 2g — both modes)** — explicit per-seat verdict against canonical role set (CEO / CTO / Head of Sales / Head of Product / CFO / Customer Success) with stage-appropriateness baked in. Pre-seed with no CFO = stage-appropriate (➖); Series A with $2M ARR and no Head of Sales = stage-inappropriate gap (🔴). Scout mode produces verdict line only; Diligence mode produces full per-seat table with soft-IC-condition candidates. **Downstream scoring updates:** `references/diligence-scoring-rubrics.md` Execution Risk expanded — separate +0/+1/+3 increments for Product-Team Fit and Market-Team Fit (replacing single PMTF rule); Network Map evidence adds +2 risk when zero connections surface; Key-Seat Completeness 🔴 gaps add +2 risk per gap (cap +3); Staff bench strength STRONG offsets founder Execution Risk by -1; founder claim verification rules retained. `references/scout-questions.md` Team scoring rewritten to consume bifurcated verdicts + Key-Seat verdict + commitment ratio; scoring table 0–5 reframed against the new sub-assessments; flag list expanded with "specify Product-side vs Market-side" when either fit lens is WEAK. **Command invocations updated:** `/scout` line 51 now passes `mode: scout` with explicit scope note; `/diligence` line 162 now passes `mode: diligence` with explicit instruction to produce Staff Deep-Dive, Network Map, and Key-Seat Completeness. Briefing artifact renamed from "Company Research Briefing" to "Team Analyst Briefing" — aligns with v2.14 naming standardization. **Plugin repackaged to v2.15.0; v2.14.0 archived to `plugin/archive/`.** **Future deferred enhancement** captured in agent prompt (Section 2c Network Map) and in memory: NWA Member Social Graph Bonus surfaces which NWA members are connected (1st/2nd degree) to founders, staff, or claimed customers — activates when Dealum API + Member Social Intelligence Layer come online. No restructure required; just additional rows in the Network Map under the existing schema. |
| v0.28.0 | May 29, 2026 | **Plugin v2.14.0 — agent naming standardization.** Four agents renamed for consistent `<descriptor>-analyst` convention, aligning with the existing `market-analyst` / `pricing-analyst` / `forecasting-analyst` / `venture-analyst` pattern. **Renames:** `company-researcher` → `team-analyst` (already had this mandate post-v2.13, name now matches); `competitive-intelligence` → `competitive-positioning-analyst`; `technical-diligence` → `technology-analyst`; `risk-assessor` → `risk-analyst`. **Briefing artifact names** updated to match agent names (e.g., `Risk Assessor Briefing` → `Risk Analyst Briefing`, `Competitive Intelligence Briefing` → `Competitive Positioning Analyst Briefing`). **Scope:** (1) 8 agent files renamed in both `.claude/agents/` and `plugin/unpacked/nwai-tech-pipeline/agents/` with internal `name:` frontmatter and "You are the NWAi X" prompt identities updated. (2) Slug references replaced across `.claude/commands/` (diligence, scout, dd-report, screen, memo), `.claude/skills/nwai-investment-framework/references/` (gates-and-flags, scout-questions, dd-report-format-reference, diligence-scoring-rubrics), `CLAUDE.md`, `nwai-techgroup-pipeline-architecture.md`, and all `docs/reference/` HTML + MD docs. (3) Current-tense operational references updated in `dd-report.md` (Risk Analyst acquirer landscape) and `diligence.md` (Competitive Positioning Analyst comp set). (4) Platform Overview HTML refreshed; PDF regenerated via Chrome headless. (5) Plugin repackaged as v2.14.0 with flat-layout zip; v2.13.1 archived to `plugin/archive/nwai-tech-pipeline-v2.13.1.plugin/.zip`. **Files retained as-is per changelog-preservation convention:** v0.22.0 architecture changelog row (historical Phase A description), `docs/build-history/Plugin-v2.13-Financial-Agent-Team.md` (build brief documenting what was named at the time), `notes/Process-Improvements-from-Synergist-Diligence-Meeting-2026-04-01.md` (process-category section header). **Behavior:** no functional change to agent capability, mandate, or output structure — purely a naming standardization. Plugin v2.14.0 reinstall is OPTIONAL (workspace `.claude/` files already live); reinstall only if `nwai-tech-pipeline.plugin` is loaded on a separate machine. |
| v0.26.0 | May 12, 2026 | **Customer Interview added as fourth diligence meeting type — Behavioral-Intensity Question Bank seeded at both pre-call and post-call surfaces.** Derived from Emily Bennett's a16z speedrun newsletter *"Early Stage Markets Rarely Look Like Markets"* (May 12, 2026), which argues *intensity of behavior, not size of user base* is the leading indicator of a real market. Closes a structural gap in NWAi's diligence framework: meetings *with the company's customers* (as opposed to with the company itself) had no dedicated playbook. **Two-surface design** — the four questions are surfaced *before* the call (via `/diligence` DD Kickoff Package Part F so the deal lead has them in hand for the live interview) and *after* the call (via `/post-meeting [Company] interview` as analytical lens against the transcript). Without the pre-call surface, post-call analysis would return weak signals on transcripts whose calls were structured around satisfaction rather than behavioral intensity. (1) **`references/diligence-analysis-framework.md`** — new section "Customer Interview — What to Look For" inserted between the Financials Meeting playbook and Progressive POV. Anchored on Bennett-derived framing: *"At NWAi's stage, traction tells you the company exists. Behavioral intensity in the customer base tells you whether the market will."* Defines the four-question Behavioral-Intensity Question Bank: (a) **Tool replacement & adoption** — *"What were you using before this product? How did you find this one and what made you switch?"* (pull vs. push adoption + switching cost); (b) **Unplanned use cases** — *"Have you found ways to use this that the vendor didn't pitch to you?"* (product malleability + category-forming behavior signal); (c) **Organic evangelism** — *"Have you talked to anyone outside your company about this product?"* (genuine word-of-mouth vs. hypothetical referral); (d) **Counterfactual loss** — *"If this product disappeared tomorrow, what would you do?"* (Memory Lock-in stickiness, Sean Ellis variant). Also defines key declaration types (adoption/stickiness/workflow/sourcing), structural contradictions to watch for, and carry-forward synthesis with Product/GTM/Financials meetings. Includes mandatory **Behavioral Intensity classification** (Strong / Some / None) — None indicates commodity status and must be flagged. (2) **`commands/post-meeting.md`** — `interview` added as fourth valid meeting type in argument-hint and parse logic. Step 3 (Analyst Lens) extended with `interview`-specific routing block: per-question capture format (what was asked / verbatim response / behavioral signal read). Step 6 (POV Box) mandatory classifications list extended with Behavioral Intensity classification requirement for `interview` meetings. (3) **`commands/diligence.md`** — Phase 4 Part F (Recommended Next Steps) extended with mandatory **Customer-Interview Prep callout** clause: when customer reference interviews appear in the Recommended DD Actions list, the action item MUST embed the four-question Behavioral-Intensity Question Bank inline as a shaded callout box — not just a pointer to the framework doc. The four questions render verbatim so the deal lead has them in hand from the DD Kickoff Package without needing to navigate to a separate reference doc. Phase 6 (docx layout spec) for Part F updated to enforce the inline callout-box rendering. Closes the workflow loop: pre-call surface (via `/diligence` output) feeds post-call analytical lens (via `/post-meeting interview`). The diligence-analysis-framework.md Customer Interview section was also updated with an explicit Pre-call / Post-call usage note at the top, making the two-surface design self-documenting. (4) **No plugin repackage required** — all edits are workspace files (command + reference + architecture doc); no MCP changes, no skill structural changes. Takes effect immediately per CLAUDE.md plugin reinstall rule. |
| v0.25.0 | May 12, 2026 | **Push customer-transcript discipline upstream — `/post-meeting` + `team-analyst` agent updated** (Round 2 of the May 12 framework evolution; follows v0.24.0 which patched the downstream commands). **`/post-meeting` Step 3 (Analyst Lens) expanded** with four new directives: (1) **Direct Quote Capture Discipline** — every Key Insight that touches a customer/partner perspective MUST carry a verbatim quote with attribution + date; paraphrases lose evidentiary weight, verbatim quotes preserve it for downstream `/dd-report` and `/memo` citation. (2) **Team signals → Founder Profile Tags** — the existing "Team signals" lens item expanded to require surfacing or updating the structured profile tag set (first-time CEO / seasoned, solo / co-founded, academic-researcher / industry operator, prior exit, full-time / other-role, operating-vs-technical depth) at the point of observation, captured as a discrete Key Insight rather than buried in generic team-signals prose. (3) **Partner / Reference Classification (Channel-vs-Acquirer discipline)** — when an external party is the subject or speaker, classify the relationship explicitly using transcript-anchored signal (Channel/Resale, Licensee/Embedder, Co-development, or Strategic acquirer with explicit M&A signal). Never default to "potential acquirer" without transcript evidence. (4) **External Operating Memo capture ("Petro Pattern")** — when a customer/partner delivers an unprompted operating playbook, capture as a distinct Key Insight tagged "External Operating Memo." Step 6 (Form the Meeting POV) now mandates partner classification + founder profile observation in the POV box when applicable. **`team-analyst` agent — new Section 2d: Founder Profile Tags** added between Section 2c (Team Commitment Depth) and Section 3 (Funding History). Produces a structured tag block per operating founder across 7 dimensions (CEO experience, founder structure, background type, prior exit, commitment, depth profile, domain origin) with archetype-aware risk read + strength read sentences. Includes a 5-row profile-archetype reference table (e.g., "First-time CEO + solo founder + academic-researcher → expect delegation gap, recommend hiring + delegation plan as soft IC term"). Agent description + Output Format template + SUMMARY FOR SCORING block all updated to reference the new profile tags. **Net effect:** customer-transcript discipline now operates upstream at meeting reconciliation time and at the team-research agent, so all downstream commands (`/dd-report`, `/memo`, `/decision`) consume pre-classified inputs instead of re-doing the work. No plugin version bump (command + agent file edits only — no MCP changes, no skill changes). |
| v0.24.0 | May 12, 2026 | **`/memo` and `/dd-report` enhanced with Customer Transcript Tier-1 Weighting protocol** (learnings from STL Panasonic May 1 reference call). Both commands now require: (1) loading post-meeting reconciliation docs and raw customer/reference transcripts alongside the DD Report; (2) anchoring Strengths/Risks (memo) and section verdicts (DD Report) in verbatim attributed quotes; (3) flagging Three-Source Corroboration when 3+ independent sources name the same insight; (4) **Channel-vs-Acquirer discipline** — never classify a strategic partner as an acquirer without explicit transcript M&A signal (LOI, advisor-led conversation, named M&A interest); default to channel/licensee, require transcript-anchored upgrade; (5) **Founder Profile Tags** — explicit tagging of first-time vs seasoned CEO, solo vs co-founded, academic/researcher vs industry operator, prior-exit yes/no, full-time vs maintains other role; tags drive Execution Risk score and Recommendation soft conditions; (6) "Petro Pattern" — when a customer/partner delivers an unprompted operating playbook, capture as outside-advisor POV and use to anchor NWAi-side soft terms. **STL DD Report regenerated as v3** (`STL-NWAi-DD-Report-2026-05-12.docx`) with two surgical corrections grounded in the May 1 Panasonic call: S8 Team verdict reframed to make first-time CEO + solo founder + UCI academic-researcher profile explicit (was implicit); S12 Exit Strategy reclassified Panasonic from "Highest-probability acquirer" to "Channel + licensee, NOT acquirer" with real acquirer candidates (Christie, Sony, Epson, Optoma, Disguise, Pixera) repositioned as primary exit path; S13 "How we win" + recBox + recRationale updated for consistency. No plugin version bump (command file edits only — no MCP changes, no skill changes). |
| v0.23.0 | May 7, 2026 | **Plugin v2.13.1 — agent sanitization + forward-facing doc refresh.** Privacy/sanitization pass plus refresh of the public-facing reference docs to v2.13. No behavior changes. **(1) Agent sanitization:** removed all NWA member name attributions (Sam Guren, Ron Tarro, Jamie Allison, Randy) and deal-specific examples (Synergist, CDW, Captain Compliance, FGO) from the four agent files (`pricing-analyst`, `forecasting-analyst`, `venture-analyst`, `team-analyst`), the `/diligence` command (Stage 2B venture-analyst prompt), and `references/diligence-scoring-rubrics.md` (35% IRR hurdle attribution). Substance and methodology preserved everywhere — McMurry method retained as external eponym (Bert McMurry / IVP); the "no AI slop" rule, 35% IRR hurdle, NWA 10x-in-5-years criterion, and three-statements forecasting mandate all retained without member attributions; channel margin compression example generalized from "CDW 8x markup vs direct 2.5x — Synergist case" to a generic distributor-markup framing. **(2) Forward-facing docs refreshed to v0.22.0 / v2.13:** `docs/NWAi-TechGroup-Pipeline-Reference.html` and `docs/NWAi-TechGroup-Platform-Overview.html` updated for header badges, 8-agent roster (PMTF refresh on team-analyst + 3-agent financial diligence team divider), Universal Triage v2.0 framing on `/screen` and pipeline-stage cards, scoring rubric descriptions reflecting v2.13 numerics. PDFs regenerated via Chrome headless. **(3) Stale-version cleanup:** two `v2.11.0` references in `CLAUDE.md` (line 374) and architecture file structure section (line 311) corrected to `v2.13.0` (the actual `plugin.json` was already at `2.13.0`). **(4) CLAUDE.md agent roster** prose (lines 224/226) and **architecture file agent descriptions** (lines 115/120/125) sanitized to match the agent layer. **Files retained as-is per scope:** architecture changelog historical entries (which legitimately document working sessions), `docs/build-history/` build briefs (explicit attendee documentation), `deals/active/` operational records. Plugin repackaged to v2.13.1; v2.13.0 archived to `plugin/archive/nwai-tech-pipeline-v2.13.0.plugin`. |
| v0.22.0 | May 7, 2026 | **Round 2 architectural evolution — plugin v2.13.0.** Following the May 6, 2026 working session with Sam Guren (NWA), Ron Tarro (NWA TechGroup), and Jamie Allison, two architectural changes shipped together. **Phase A — Company Researcher refocus:** `team-analyst` agent rewritten with team-first mandate. New sections: Founder Claim Verification Protocol (LinkedIn + Perplexity cross-reference of every specific exit/ARR/role claim, with ✅ VERIFIED / ⚠️ PARTIAL / 🔴 UNVERIFIED / ❌ CONTRADICTED states), Team-Level PMTF Synthesis (skills coverage across domain depth / engineering / market access, with explicit gap detection per Ron Tarro), Team Commitment Depth (full-time vs advisor ratio with "stuck" advisor flagging per Sam Guren), advisor capital commitment check. `references/scout-questions.md` Team scoring expanded to consume PMTF + commitment + verification sub-assessments. `references/diligence-scoring-rubrics.md` Execution Risk now includes explicit numeric criteria for team commitment ratio, PMTF score, and founder claim verification. **Phase B — Financial agent team:** Legacy `financial-analyst` retired and replaced by three specialized agents that work as a system. (1) `pricing-analyst` (NEW) — pricing maturity assessment, unit economics, channel economics with margin compression forecast (e.g., CDW 8x markup vs direct 2.5x), value proposition validation with pricing-to-value ratio, pricing pressure forecast (commoditization risk). (2) `forecasting-analyst` (renamed from `financial-analyst`, full rewrite) — applies the McMurry method (per Sam Guren: build proprietary forecast first, then compare to company's submitted numbers); enforces the "no AI slop" rule (per Jamie Allison: critical lens, independent research, accountable analyst POV with mandatory *because* clauses); produces 5-year P&L + cash flow + balance sheet with Bear/Base/Bull scenarios; capital plan with round timing/sizing; founder financial literacy assessment. (3) `venture-analyst` (NEW) — final synthesis layer; consumes pricing + forecasting + competitive + risk + team outputs to produce defensible valuation today (3 methods reconciled), projected exit Y3+Y5, capital + dilution modeling, IRR / multiple, 35% hurdle rate test (per Sam Guren), NWA 10x-in-5-years criterion check, deal structure recommendation (priced equity / convertible with cap with "industry of 8" reference / participating preferred / re-priced terms / decline). Solves the Randy "$9M-claimed-vs-$3M-analysis" problem with documented methodology. **/diligence command** restructured into Stage 2A (6 agents in parallel: 5 standard + pricing-analyst) and Stage 2B (sequential financial diligence chain: forecasting-analyst → venture-analyst). **/dd-report** Section score mappings updated: S5 (GTM) consumes pricing-to-value ratio + channel pressure forecast; S6 (Team) consumes PMTF score + Team Commitment Depth + verification status; S8 (Deal Structure) consumes venture-analyst structure recommendation; S9 (Financials) consumes forecasting-analyst Bear/Base/Bull + pricing-analyst unit economics + venture-analyst capital plan; S11 (Exit) consumes venture-analyst exit valuation + 35% hurdle test + 10x criterion + risk-analyst acquirer landscape. **/decision** INVEST/PASS reasons expanded with hurdle test results and PMTF/verification flags; Decision Summary captures venture-analyst structural recommendation. **/memo** Pro forma now sourced from forecasting-analyst's proprietary forecast (NOT company's spreadsheet); Slide 4 Returns and Deal Terms reference venture-analyst outputs; Slide 4 Strengths/Risks reference Phase A Team Commitment + verification status. **Bear/Base/Bull rubric** updated with mandatory *because* clauses and time-varying ARPU (channel pressure modeling). **CLAUDE.md** agent roster updated to 8 agents (3 new financial + Phase A team-analyst refresh). Companion reference doc shipped at `docs/build-history/Plugin-v2.13-Financial-Agent-Team.md`. Round 2 review with Sam/Ron does NOT gate the v2.13.0 ship; iterative refinement is post-ship. |
| v0.21.0 | Apr 28, 2026 | **Dealum integration deferred — plugin v2.12.0 repackaged without MCP server registration.** The `nwai-tech-pipeline` plugin had been declaring a Dealum MCP server (`nwai-dealum` in `.mcp.json`) that requires Python 3.10+ and the `mcp` package, plus `DEALUM_TOKEN` / `DEALUM_ROOM_ID` env vars. None of these prerequisites were in place locally, causing Cowork to show "MCP nwai-tech-pipeline: Server disconnected" on plugin enable, which obscured the fact that all 8 slash commands were otherwise functional. Decision: rather than upgrade Mac Python and install dependencies for an integration that's not yet API-approved, remove the MCP server registration so the plugin enables cleanly and the pipeline operates filesystem-first (which is how all actual deal work has been done to date). Changes: (1) Plugin repackaged from v2.11.0 to v2.12.0 — bundled `.mcp.json` updated to `{"mcpServers": {}}` with a `_comment` field documenting restoration procedure; bundled `.claude-plugin/plugin.json` description softened to reflect filesystem-first operation; v2.11.0 archived to `plugin/archive/nwai-tech-pipeline-v2.11.0.plugin`. (2) Workspace `.mcp.json` updated to match (empty mcpServers + restoration comment). (3) `CLAUDE.md` — new "Dealum Integration Status — Deferred" section added between Plugin Architecture and GitHub Sync; states current filesystem-first reality, lists 4 implications (no MCP server, /sync-pipeline operates on filesystem, Dealum-tagging guidance is aspirational, dealum_server.py is preserved dormant), and provides 5-step restoration procedure for when the API is eventually approved. (4) Architecture file Layer 1 rewritten from "The Data Source: Dealum via MCP" to "The Data Source: Local Filesystem (Dealum deferred)" with parallel "Active state" / "Deferred state" framing. (5) The `dealum_server.py` script itself is preserved verbatim in `.claude/servers/` and in the bundled plugin so restoration is mechanical. After this change, plugin enables cleanly in Cowork with no MCP server error, all 8 commands and 7 agents register normally. `/sync-pipeline` and `pipeline-monitor` should be reframed as filesystem-readers in a follow-up if they're actually used; today they're dormant. |
| v0.20.0 | Apr 28, 2026 | **Workspace consolidation — Desktop → canonical merge** (no plugin code change). (1) Surgical gitignore enforcement: `git rm --cached -r deals/` removed 43 previously-tracked deal files (term sheets, cap tables, financials, contracts, transcripts) from git tracking; deal files remain on local disk; `.gitignore` now correctly enforces deals-stay-local intent going forward. Note: files remain in git history; full history rewrite intentionally not performed. (2) Strategic platform-vision docs migrated from prior Desktop workspace `~/Desktop/Claude CoWork NWAi Investment Intelligence/docs/` into new `docs/strategy/` subfolder (10 files: GUT v0.1, GUT html/pdf, Strategic Reframe, Huddle Brief, Member Social Intelligence Layer, RaiseLink comparison, Managed Agents Architecture, cowork-vs-enterprise-platform, Jamie & Ron GUT chat). (3) Plugin build-session briefs migrated from Desktop `Claude Code/` into new `docs/build-history/` subfolder (6 files: Scoping, Setup, Session-Backlog, Session-2/3 Briefs, Desktop-Checkout-Reconciliation-Report). (4) Captain Compliance deal — entire `Data Room/` + `Reports/` migrated from Desktop into `deals/active/Captain Compliance/`. (5) Synergist Technology — Desktop's newer artifacts (Apr 14 GTM Diligence, updated Action Tracker, GTM call transcript, additional license agreements and contracts) merged with canonical's existing Synergist content into nested `deals/active/Synergist Technology/{Data Room,Reports}/`. (6) STL reorganized from flat layout to nested `deals/active/Summit Technology Laboratory/{Data Room,Reports}/`. (7) Redundant pre-consolidation flat-layout files moved to `deals/_quarantine_pre_consolidation_2026-04-28/` for manual cleanup. (8) `CLAUDE.md` updated: "Working in Cowork — Which Folder to Select" rewrites the misleading "Desktop is a planning archive" framing into the true consolidation story; "Workspace Files" section expanded with full canonical layout including new `docs/strategy/` and `docs/build-history/` subfolders; explicit `deals/` is local-only note added; "GitHub Sync" section softened to acknowledge Cowork sandbox push limitations and Mac Terminal fallback. (9) Architecture file structure section updated to reflect new layout. (10) Migration plan archived at `notes/folder-consolidation-plan-2026-04-28.md`. After this commit, Cowork should only ever target `/Users/jamie/ClaudeCodeProjects/nwa-intelligence/`; the Desktop folder will be archived. |
| v0.19.0 | Apr 2026 | **Companion demo artifact added to architecture file** (no plugin change). New "Companion Artifact — NWAi Investment Intelligence Demo" section documenting the Next.js / Tailwind v4 / shadcn IA preview at `demo/`: Session 3 surfaces (Pipeline, Members + collapsible-filter directory, Member Profile, Matching Rationale, Portfolio + slide-over drill-in, Ecosystem Phase 4 placeholder, Orchestrator), top-level SectionNav above the 7-stage StageNav, FL-city geography facet, Universal Triage v2.0 reflected on Pipeline stage cards. Captures deployment pattern (Vercel, Root Directory = `demo/`, auto-deploy from `main`) and the demo-vs-plugin decoupling: plugin tracked in this changelog, demo tracked through commits under `demo/`. Plugin source itself unchanged in this update. |
| v0.1.0 | Feb 2026 | Initial plugin architecture — 5 pipeline stages, 6 commands, 5 reference docs |
| v0.2.0 | Mar 2026 | Added `/dd-report` command + `dd-report-format-reference.md` + `memo-format-reference.md`; updated pipeline to 7 stages including DD Report between Diligence and Decision |
| v0.3.0 | Mar 2026 | Finalized DD Report stage in all layers (CLAUDE.md, SKILL.md, plugin.json v0.2.0); fixed dynamic path resolution in dd-report.md command; updated Output Depth table; DD Report now the primary IC briefing document, Memo repositioned as archival/external presentation |
| v0.4.0 | Mar 2026 | Updated `/scout` command (plugin v2.1.0) to generate a Scout Assessment Report .docx in addition to the in-chat report; added `Bash` and `Write` to scout command allowed-tools |
| v0.5.0 | Mar 2026 | All 5 output-producing commands now save to `deals/` subfolder (plugin v2.2.0); added .docx file output to `/screen`; upgraded `/diligence` from optional .md to always-on .docx; fixed hardcoded session path in `/memo`; moved existing Synergist deal files to deals/ |
| v0.6.0 | Mar 2026 | Reformed TechGroup screener (plugin v2.3.0): replaced 6-gate AutoKill with 3-layer triage framework (3 hard gates + Opportunity/Readiness scoring); added `gates-and-flags-techgroup.md`; SAFE and lead investor moved from hard gates to Readiness signals; ADVANCE threshold ≥ 18/25 Opportunity; WATCH requires named milestone; ADVANCE output includes Live Pitch Questions; light WebSearch research on Market + Founder dimensions |
| v0.7.0 | Mar 2026 | Added prior stage output loading to Scout, Diligence, DD Report, and Memo commands (plugin v2.4.0): each downstream stage now explicitly loads the most recent output files for prior stages from deals/ before running; loading chain is Screen→Scout→Diligence→DD Report→Memo; most recent version by date is used when multiple versions exist; each command notes version count and load date; missing files are noted but do not block execution |
| v0.8.0 | Mar 2026 | Fixed Part B scoring in `/diligence` command (plugin v2.6.0): (1) Tier 1 General Moat now requires all 6 dimensions shown individually with ✓/✗ and rationale before summing — prevents collapsed binary output; (2) Tier 2 AI Moat applicability gate added to both diligence.md and diligence-scoring-rubrics.md — non-AI companies marked N/A with one-line reason, gate added to rubrics source of truth; (3) Tier 2 output now shows sub-elements labeled by category with points awarded/available; (4) Risk section heading now includes direction annotation (1=lowest, 10=highest) at section level only; (5) General N/A rule added — any sub-rubric inapplicable to company type must be marked N/A, not force-applied |
| v0.9.0 | Mar 2026 | Refactored `/diligence` DD Kickoff Package output format to Hub & Spoke structure: (1) Part A — removed "Key Risks" section; replaced with single gate-critical callout line referencing Part C folder numbers; (2) Part C — renamed third column from "Assignee" to "Status / Key Question"; gate-critical and urgent folders now embed the single most important question inline with flag and color shading (red=gate-critical, amber=urgent); Part C is now the single source of truth for all risks and questions; (3) Part D — removed "Gate-Critical" questions block; Part D now contains Scoring Inputs and Nice to Have only; opens with callout note pointing to Part C; (4) Part E — removed "Conditions That Must Be Met" prose block and "If Conditions Cannot Be Met" prose block; replaced with compact Kill Conditions table (Condition \| Trigger \| Verdict, 2–4 rows); (5) Fixed hardcoded session path in Phase 6 docx skill read — now uses dynamic find command |
| v0.10.0 | Mar 2026 | Overhauled `/dd-report` command and `dd-report-format-reference.md` (plugin v2.7.0): (1) Corrected DD Report visual layout to exact STL spec — every scored section is a 2-column table (content col 9432 DXA, score col 1080 DXA), not paragraph + callout box; (2) Added RAG score cell colors extracted from STL source XML (Green `375623`/White for 4–5, Amber `FFC000`/Dark for 3, Red `C00000`/White for 1–2) at 24pt bold; (3) Set exact page layout (margins 864 DXA, content width 10512 DXA, header/footer offset 708 DXA); (4) Added section numbering requirement (1. through 11.); (5) Corrected document structure order: Recommendation banner → Company header → Sections 1–11 → Recommendation table → DD Team Votes → Appendix A; (6) Added 5 Sharp & Succinct content rules to dd-report.md Step 4 (Once and Down, Section Mandates Exclusive, Tables Absorb Facts, Risk Synthesizes not Repeats, No Closing Restatements); (7) Added format drift incident log to dd-report-format-reference.md; (8) Added mini-table specifications per section; (9) Designated `STL-NWAi-DD-Report-2026-03-19.docx` as canonical visual reference (supersedes prior Synergist reference) |
| v0.11.0 | Mar 2026 | Scout framework enrichment based on Ron Tarro framework review session 2026-03-22 (plugin v2.7): (1) Full rewrite of `scout-questions.md` — renamed Q1 to Category & Market Discontinuity (new category creator vs. optimizer; lifecycle horizon; structural shift test; 0–5 with Triage delta); Q3 Moat Assessment now produces a distilled 4-column table verdict (Primary Moat \| Strength \| Primary Threat \| Verdict) instead of moat-type enumeration by number; three new strategic dimensions added at Scout (Q4: Ecosystem Role — platform creator vs. follower, 0–5; Q5: Adjacent Displacement Risk — core use case + functional equivalents + emerging displacement, 0–5 inverted; Q6: Macro Tailwind — 4-dimension 10-year horizon table, 0–5); Phase 2 Team assessment now requires explicit Product team fit (✓/Partial/Gap) and Market team fit (✓/Partial/Gap); Analyst Verdict Block expanded to include "What You Have to Believe" (core thesis assumption) + "Where's the Bet" (specific inflection point) + "Greed" (upside case paired with Fear); Scout Conviction Score composite (/17) added (Phase 1: 40% / Strategic: 20% / Phase 2: 40%; conviction thresholds 14–17=High, 10–13=Moderate, 7–9=Low, <7=Decline); 2-page output format enforced (Page 1: scorecard tables only — Triage Carry-Forward + Product & Market Positioning + Moat Assessment + Macro Trends + Analyst Verdict Block + Score Summary with ↑/→/↓ Triage deltas; Page 2: Adjacent Tech bullets + Phase 1 bullet clusters + Phase 2 execution table + Flags + Diligence Questions); (2) `gates-and-flags-techgroup.md` — added Scout forward-mapping notes to Dimension 1 (Structural Discontinuity → Q1 Category & Market Discontinuity) and Dimension 4 (Defensibility → Q3 Moat + Q4 Ecosystem Role + Q5 Adjacent Displacement Risk); (3) `diligence-scoring-rubrics.md` — added Adjacent Displacement Risk as explicit sub-dimension of Competitive Risk (carry forward Q5 score from Scout); (4) `scout.md` command — fully updated to match new 8-step framework, fixed hardcoded old session path for docx skill (now dynamic find), Word doc output updated to 2-page design with 14 sections, output path updated to `deals/active/` subfolder |
| v0.12.0 | Mar 2026 | Diligence Layer 2 — Hypothesis Confirmation Plan (plugin v2.8): (1) No Scout → No Diligence hard gate added to `/diligence` command — halts if Scout Assessment Report not found in workspace; post-Dealum-API: stage tag check to be added as second verification; (2) Pre-flight confirmation prompt added — requires user CONFIRM before launching agent team; reminds user to verify financial files in deal room; (3) Agent team expanded from 2 to 6: team-analyst (enhanced with commercial validation), market-analyst (new — discontinuity test + TAM/SAM + timing), competitive-positioning-analyst (refocused to pure competitive), technology-analyst (new — thin wrapper + TRL + IP + AI moat), financial-analyst (new — reads deal room financial files, models unit economics + 10x path), risk-analyst (new — regulatory, exit landscape, execution + market risk); (4) Agent deployment by stage: team-analyst + market-analyst + competitive-positioning-analyst + technology-analyst (light) at Scout; all 6 at Diligence; financial-analyst conditional on financial files; (5) DD Kickoff Package restructured from 5 parts (A–E) to 6 parts (A–F): Part B is new Layer 2 Hypothesis Confirmation Plan (AI-derived conclusions, not human-filled); Part C is Scored Assessment (formerly B); Part D is Layer 1 17-folder checklist (formerly C, renamed); Part E and F carry forward former D and E; (6) Layer 2 output format enforced: each of 6 validation groups = hypothesis (from Scout) + 2–3 sentence AI conclusion + 🟢/🟡/🔴 signal + biggest uncertainty; strict 4–5 line maximum per group, 1-page total; (7) `dd-checklist.md` updated with Layer 2 architecture overview and Layer 2 → DD Report score mapping table; (8) `diligence-scoring-rubrics.md` updated with Layer 2 rubric mapping table (each rubric mapped to its primary + secondary Layer 2 group); (9) Scout command updated to launch 4 research agents (team-analyst, market-analyst, competitive-positioning-analyst, technology-analyst light) before Phase 1 scoring |
| v0.18.0 | Apr 2026 | Triage-with-scoring promoted to NWAi Universal Screening Framework (Backlog #1): (1) `references/gates-and-flags.md` rewritten — was the legacy 6-gate pass/fail AutoKill (107 lines); now the **NWAi Universal Triage Framework v2.0** (262 lines): 3 hard gates + NWA Filter (Cynical Default, Goliath Test, LLM Ingestion Test, Revenue Quality Audit) + Opportunity scoring (6 dimensions × 0–5 = 30) + Readiness scoring (5 dimensions × 0–5 = 25) + Readiness Downgrade Rule + Research Protocol + Decision Logic (ADVANCE ≥ 20/30, WATCH 14–19, DECLINE < 14 or any hard gate FAIL or Market Opportunity ≤ 2). Applies to all NWAi verticals (Tech, Medical, Space, Consumer, Industrial, Fintech); (2) `references/_archive/gates-and-flags-6gate-legacy.md` created — preserves the original 6-gate AutoKill content (107 lines, banner-flagged as deprecated) for historical reference; (3) `references/gates-and-flags-techgroup.md` slimmed from 437 → 260 lines — now strictly the TechGroup extension: Track A (Software/AI/Cloud) and Track B (Hardware/Robotics) 6-dimension Opportunity rubrics, AI Wrapper Assessment, Replicability Speed Flag, Hardware Last Mile Standard, TRL Hard Cap, TechGroup Live Pitch Questions; cross-reference banner at top points to universal `gates-and-flags.md`; (4) `CLAUDE.md` updated — Pipeline section "Screening" stage definition rewritten to describe Universal Triage Framework with TechGroup Track A/B extension; "On failed AutoKill gates" guidance reworded to "On failed Hard Gates" including Market Opportunity ≤ 2 sub-floor; `/screen` command description updated; Key Frameworks section split into Universal + TechGroup Extension + Legacy archive entries; Terminology table replaces "AutoKill" entry with "Universal Triage Framework" + retains "AutoKill (legacy)" pointer to archive; (5) Both mirrors verified identical (`.claude/` and `plugin/unpacked/nwai-tech-pipeline/skills/...`). Decision: LLM Ingestion Test kept in universal NWA Filter (Option A); Track A/B bifurcation remains TechGroup-only (Decision 2). |
| v0.17.0 | Apr 2026 | Workspace hygiene + canonical-checkout consolidation (Backlog #2): (1) `CLAUDE.md` Workspace path on Mac corrected from `/Users/jamie/Desktop/Claude CoWork NWAi Investment Intelligence` to `/Users/jamie/ClaudeCodeProjects/nwa-intelligence` — the Desktop folder is the Cowork planning workspace, not the canonical code checkout; (2) `.gitignore` updated to ignore `deals/` — deal artifacts (pitch decks, transcripts, Triage/Scout/DD reports) are local file organization only, not version-controlled, eliminating layout-consistency questions across STL/Synergist/Captain Compliance; (3) Bundled with destructive cleanup commit removing Drive shadows (`plugin/current/zi*`), legacy push scripts (`.git-autopush.sh`, `push.sh`), and the redundant Desktop `.git/` checkout that was Drive-syncing a credential-bearing config; (4) Note: post-PAT-rotation update to `CLAUDE.md` GitHub Sync section deferred until Jamie completes token rotation. |
| v0.16.0 | Apr 2026 | Architecture file corrections — gap closure after recent plugin updates: (1) Fixed Layer 3 heading from "Seven Workflows" to "Eight Workflows" (post-meeting was the 8th); (2) Fixed Layer 5 reference count from "Seven" to "Nine"; (3) Added `post-meeting.md` to file structure commands/ listing (was in Layer 3 table but missing from tree); (4) Added `gates-and-flags-techgroup.md` to file structure references/ listing (was in Layer 5 table but missing from tree); (5) Added `diligence-analysis-framework.md` to file structure references/ listing (same gap); (6) Added retroactive v0.15.0 changelog entry |
| v0.15.0 | Apr 2026 | Added `/post-meeting` command and `diligence-analysis-framework.md` reference (plugin v2.9.x): (1) `commands/post-meeting.md` — new command for processing diligence meeting transcripts; applies analyst lens (declarations, structural contradictions, moat signals, team signals, thesis stress points) before reconciling Diligence Action Tracker; produces dual-output document (Analyst POV + Key Insights + Resolved/Open tracker); triggered via `/post-meeting [company] [product\|gtm\|financials]`; (2) `references/diligence-analysis-framework.md` — new reference doc defining the full behavioral standard for post-meeting analysis; analyst POV leads every output; tracker reconciliation is the record, not the deliverable; progressive POV architecture — each meeting updates the running thesis; (3) Layer 3 commands table updated to include `/post-meeting` row; Layer 5 reference table updated to include `diligence-analysis-framework.md` row; file structure tree not yet updated (corrected in v0.16.0) |
| v0.14.0 | Apr 2026 | Framework enrichment from Synergist diligence meeting feedback (A1 + A2): (1) `scout-questions.md` — added Q1b Demand Signal Test as new Phase 1 dimension (demand-pull vs. technology-push assessment; required outputs: demand type, evidence, strongest signal; 0–5 scoring rubric; ⚠️ Yellow Flag at score ≤ 2; agent support note for market-analyst); Phase 1 now 4 dimensions (Q1 + Q1b + Q2 + Q3); Scout Conviction Score recalculated — Phase 1 weighted max increased from 6.0 to 8.0, total composite now /19 (was /17); conviction thresholds updated (16–19=High, 11–15=Moderate, 7–10=Low, <7=Decline); Score Summary table and Page 2 rationale updated to include Q1b row; (2) `ai-moats-framework.md` — added Replicability Speed Matrix (4-row threat actor table: open-source community, funded startup, Big Tech platform, LLM provider) with required "Could Replicate In" and "Barrier" columns; added dual flag triggers (⚠️ Yellow Flag if any row < 6 months, ⚠️ Strong Yellow Flag if LLM provider row < 12 months); renumbered scoring step to 6; (3) `gates-and-flags-techgroup.md` — added Replicability Speed Flag sub-section under AI Wrapper Assessment (applies at Scout + Diligence); two flag triggers matching ai-moats-framework; compounding rule: High Wrapper Risk + Replicability Speed Flag = strong DECLINE signal unless SME-validated |
| v0.13.0 | Mar 2026 | Architecture file housekeeping (plugin v2.9.0): (1) Updated workspace root file structure — plugin now versioned as `nwai-tech-pipeline.plugin` v2.9.0 (no version suffix in filename); added `plugin/current/` and `plugin/archive/` directories; added `nwai-tech-pipeline.zip` (org upload package); (2) Added `scripts/` directory to file structure — `scripts/dd-report-generator.js` is the mandatory canonical DD Report generator (all `/dd-report` runs must use this script); (3) Updated canonical DD report reference throughout from `Synergist-DD-Investment-Report-2026-03-11.docx` to `STL-NWAi-DD-Report-2026-03-19.docx` (CLAUDE.md binding reference); (4) Fixed changelog ordering — v0.11.0 was logged after v0.12.0 due to retroactive addition; order corrected |

---

*NWAi Investment Intelligence | TechGroup | Jamie, Co-Chair*
